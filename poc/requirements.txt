# API
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic>=2.10
pydantic-settings>=2.6

# RAG — Vector Store + Embeddings locais
chromadb>=1.0
sentence-transformers>=3.3

# LLM (OpenAI-compatible — funciona com Groq, OpenAI, Ollama)
openai>=1.57

# Utilitários
python-dotenv>=1.0
