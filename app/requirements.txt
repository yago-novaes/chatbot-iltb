# Produção — pinned quando Docker image estiver estável
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
pydantic-settings>=2.0
openai>=1.0
sentence-transformers>=3.0
chromadb>=1.0
docling>=2.0
# Avaliação RAGAS (eval/)
ragas>=0.2
datasets>=2.0
langchain-openai>=0.1
