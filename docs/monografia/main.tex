% ─────────────────────────────────────────────────────────────────────────────
%  Monografia de TCC — Chatbot de Apoio Clínico para Manejo da ILTB
%  Autor      : Yago Novaes Neves
%  Orientador : <preencher>
%  LabPEC / DTI / UFES
%  Classe     : abnTeX2 (monografia, 12pt, ABNT NBR 14724)
%  Citações   : ABNT NBR 6023 (sistema autor-data, ALF)
% ─────────────────────────────────────────────────────────────────────────────

\documentclass[
  12pt,
  oneside,
  a4paper,
  english,
  brazil
]{abntex2}

% ─── Codificação e fonte ─────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{mathptmx}           % Times New Roman

% ─── Pacotes essenciais ───────────────────────────────────────────────────────
\usepackage{indentfirst}
\usepackage{color}
\usepackage[dvipsnames]{xcolor,colortbl}
\usepackage{graphicx}
\usepackage{ifthen}
\usepackage{array}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage[singlelinecheck=false]{caption}
\usepackage{subcaption}
\usepackage{threeparttable}
\usepackage{enumitem}
\usepackage{xspace}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc}
\usepackage{newfloat}
\DeclareFloatingEnvironment[
  name={Quadro},
  listname={Lista de Quadros},
  placement=htbp
]{quadro}
% ─── Citações ABNT ───────────────────────────────────────────────────────────
\usepackage[alf,
  abnt-full-initials=no,
  abnt-emphasize=bf,
  abnt-and-type=e,
  abnt-url-package=url,
  abnt-repeated-author-omit=yes,
  abnt-etal-cite=3,
  abnt-etal-list=3
]{abntex2cite}

\newcommand{\bibtextitlecommand}[2]{%
\ifthenelse{\equal{#1}{inproceedings}}{\normalfont{\textbf{#2} $[\ldots]$}}{}}

\usepackage{url6023}

% ─── Hyperlinks (abntex2 já carrega hyperref — não recarregar) ───────────────
\hypersetup{
  hidelinks,
  pdfsubject={Monografia de TCC — Chatbot RAG para Apoio ao Manejo da ILTB},
  pdfcreator={LaTeX with abnTeX2},
  pdfkeywords={RAG}{LLM}{ILTB}{tuberculose latente}{enfermagem}{LGPD},
  bookmarksdepth=4
}

% ─── Marcadores de revisão (implementação leve — sem TikZ/todonotes) ─────────
% \aluno{} aparece em verde no rascunho; para a versão final, comente as duas
% linhas abaixo e descomente as duas seguintes (que tornam os marcadores invisíveis).
\newcommand{\aluno}[1]{{\color{olive!80!black}\textbf{[\,}{\small\itshape PREENCHER: #1}\textbf{\,]}}}
\newcommand{\professor}[1]{{\color{red!70!black}\textbf{[\,}{\small\itshape PROF: #1}\textbf{\,]}}}
% Versão final (sem marcadores visíveis) — descomentar e comentar as linhas acima:
% \newcommand{\aluno}[1]{}
% \newcommand{\professor}[1]{}

% ─── Informações da monografia ──────────────────────────────────────────────
% ATENÇÃO: \titulo, \orientador, \instituicao e \preambulo são usados dentro
% de boxes na capa/folha de rosto. Não usar \aluno{} aqui — substitua os
% textos em MAIÚSCULAS pelos valores reais antes de compilar para entrega.
\titulo{Chatbot de Apoio Clínico para o Manejo da Infecção Latente por
  \textit{Mycobacterium tuberculosis} (ILTB): uma abordagem baseada em
  Recuperação com Geração Aumentada (RAG)}
\autor{Yago Novaes Neves}
\local{Vitória}
\data{2026}
\orientador{Prof. Dr. Wilian Hiroshi Hisatugu}
\coorientador{Prof. Dr. Renato Elias Nunes de Moraes}
\instituicao{%
  Universidade Federal do Espírito Santo -- UFES \\
  Centro Tecnológico -- Departamento de Engenharia de Produção (LabPEC/DTI)}
\tipotrabalho{Trabalho de Conclusão de Curso}
\preambulo{Trabalho de Conclusão de Curso apresentado ao Curso de Engenharia
  de Produção da Universidade Federal do Espírito Santo como requisito parcial
  para obtenção do grau de Bacharel em Engenharia de Produção.}

% ─────────────────────────────────────────────────────────────────────────────
\begin{document}
\selectlanguage{brazil}
\frenchspacing

% ═════════════════════════════════════════════════════════════════════════════
% ELEMENTOS PRÉ-TEXTUAIS
% ═════════════════════════════════════════════════════════════════════════════
\pretextual

% ─── Capa ────────────────────────────────────────────────────────────────────
\imprimircapa

% ─── Folha de rosto ──────────────────────────────────────────────────────────
\imprimirfolhaderosto

% ─── Folha de aprovação ──────────────────────────────────────────────────────
\begin{folhadeaprovacao}
  \begin{center}
    {\ABNTEXchapterfont\large\imprimirautor}

    \vspace*{\fill}\vspace*{\fill}

    {\ABNTEXchapterfont\bfseries\Large\imprimirtitulo}

    \vspace*{\fill}

    \imprimirpreambulo

    \vspace*{\fill}

    Vitória, \_\_\_\_ de \_\_\_\_\_\_\_\_\_\_ de 2026.

    \vspace*{\fill}

    \assinatura{\textbf{\imprimirorientador} \\ Orientador --- UFES}
    \assinatura{\textbf{\imprimircoorientador} \\ Coorientador --- UFES}
    \assinatura{\textbf{NOME DO MEMBRO EXAMINADOR} \\ INSTITUIÇÃO}

    \vspace*{\fill}
  \end{center}
\end{folhadeaprovacao}

% ─── Dedicatória (opcional) ──────────────────────────────────────────────────
% \begin{dedicatoria}
%   \vspace*{\fill}
%   \centering\noindent
%   \aluno{Texto de dedicatória.}
% \end{dedicatoria}

% ─── Agradecimentos (opcional) ───────────────────────────────────────────────
% \begin{agradecimentos}
%   \aluno{Texto de agradecimentos.}
% \end{agradecimentos}

% ─── Resumo em português ─────────────────────────────────────────────────────
\begin{resumo}

A Infecção Latente por \textit{Mycobacterium tuberculosis} (ILTB) é um
desafio persistente para a saúde pública: cerca de um quarto da população
mundial está infectada, e o controle da progressão para tuberculose ativa
em grupos vulneráveis é estratégico para a eliminação da TB como problema
sanitário. Profissionais de enfermagem atuam na linha de frente do
rastreamento e tratamento da ILTB, mas enfrentam protocolos extensos e
periodicamente atualizados, nem sempre de fácil consulta durante o
atendimento. Este trabalho propõe, desenvolve e avalia um \textit{chatbot}
de apoio clínico baseado na arquitetura de Recuperação com Geração
Aumentada (RAG), capaz de responder a perguntas sobre o manejo da ILTB a
partir de documentos oficiais do Ministério da Saúde e da Organização
Mundial da Saúde, em português brasileiro. Seguindo a metodologia
\textit{Design Science Research Methodology} (DSRM), o artefato foi
construído como aplicação \texttt{FastAPI} com banco de vetores
\texttt{ChromaDB}, \textit{embedding} multilíngue \texttt{BAAI/bge-m3}
(1024 dimensões), busca híbrida BM25+denso com \textit{Reciprocal Rank
Fusion}, reranking \textit{cross-encoder} mGTE e modelo gerador Llama 3.3
70B acessado via API Groq sem retenção de \textit{prompts}. A avaliação
automatizada com o \textit{framework} RAGAS foi conduzida sobre 38
questões clínicas em sete categorias temáticas, replicada em três
\textit{seeds} independentes por configuração. O gate final atingiu
\textit{context precision} de $0{,}956 \pm 0{,}009$ (PASS robusto sobre a
meta de $0{,}75$, com margem absoluta de $+0{,}206$) e
\textit{faithfulness} de $0{,}752 \pm 0{,}004$, próximo do teto comum
reportado pela literatura de RAG clínico ($\sim 0{,}75$). Uma ablação
$2 \times 2$ cruzando gerador (Llama \textit{vs.} GPT-4o) e
\textit{embedding} (MiniLM 384D \textit{vs.} BGE-M3 1024D) identificou o
\textit{embedding} como variável dominante e revelou um achado paradoxal:
modelos mais aderentes a instruções de \textit{fallback} (GPT-4o) pontuam
\emph{pior} em \textit{faithfulness}, indicando que a métrica RAGAS
penaliza abstenção cautelosa --- comportamento desejável em apoio
clínico. Por essa razão, adota-se \textit{Context Precision} como
indicador-síntese do gate final, e \textit{Faithfulness} como métrica
auxiliar lida em conjunto com a discussão do paradoxo. \textbf{A
avaliação qualitativa com especialista (OE4) está protocolada e prevista
para execução no período de defesa; seus resultados serão integrados em
versão pós-defesa.} As principais contribuições do trabalho são: (i) um
artefato RAG aberto para ILTB em português, com código-fonte disponível;
(ii) um \textit{dataset} de avaliação clínica de 38 questões com
\textit{ground truths} extraídos verbatim dos protocolos; (iii) análise
empírica do \textit{trade-off} entre conformidade com a LGPD e desempenho
de recuperação; e (iv) um protocolo de avaliação multidimensional para
RAG clínico com $N$ reduzido de avaliadores, combinando RAGAS, rubrica
clínica e entrevista pelas dimensões do TAM-AIN.

\vspace{\onelineskip}
\noindent\textbf{Palavras-chave:} tuberculose latente; ILTB; chatbot clínico;
recuperação com geração aumentada; RAG; grandes modelos de linguagem; enfermagem;
LGPD.
\end{resumo}

% ─── Abstract (em inglês) ────────────────────────────────────────────────────
\begin{resumo}[Abstract]
\begin{otherlanguage*}{english}

Latent tuberculosis infection (LTBI) is a persistent public-health
challenge: about one quarter of the global population is infected, and
preventing progression to active TB in vulnerable groups is strategic for
disease elimination. Nursing professionals lead LTBI screening and
treatment, yet must navigate long, periodically updated protocols that
are not always easy to consult bedside. This work proposes, develops, and
evaluates a clinical support chatbot based on Retrieval-Augmented
Generation (RAG) for LTBI management, grounded in official documents
from the Brazilian Ministry of Health and the World Health Organization,
in Brazilian Portuguese. Following the Design Science Research
Methodology (DSRM), the artifact was built as a \texttt{FastAPI} web
application with a \texttt{ChromaDB} vector store, multilingual
\texttt{BAAI/bge-m3} embeddings (1024 dimensions), hybrid BM25+dense
retrieval with Reciprocal Rank Fusion, mGTE cross-encoder reranking, and
the Llama 3.3 70B language model accessed via the Groq API under a
no-prompt-retention policy. Automated evaluation using the RAGAS
framework was performed over 38 clinical questions in seven thematic
categories, replicated across three independent seeds per configuration.
The final gate achieved \textit{context precision} of $0.956 \pm 0.009$
(robust PASS over the $0.75$ target, absolute margin of $+0.206$) and
\textit{faithfulness} of $0.752 \pm 0.004$, near the common ceiling
reported for clinical RAG without self-reflection ($\sim 0.75$). A
$2 \times 2$ ablation crossing generator (Llama \textit{vs.} GPT-4o) and
embedding (MiniLM 384D \textit{vs.} BGE-M3 1024D) identified the
embedding as the dominant variable and revealed a paradoxical finding:
models more compliant with instructions to \textit{fallback} (GPT-4o)
score \emph{worse} on faithfulness, indicating that RAGAS penalizes
cautious abstention --- a desirable behaviour in clinical support. For
this reason, \textit{Context Precision} is adopted as the headline
indicator of the final gate, while \textit{Faithfulness} is read jointly
with the discussion of the paradox. \textbf{Qualitative evaluation with a
domain specialist (specific objective OE4) is protocolled and scheduled
for the defence period; results will be integrated in a post-defence
version of this monograph.} The main contributions are: (i) an open RAG
artifact for LTBI in Portuguese, with public source code; (ii) a clinical
evaluation \textit{dataset} of 38 questions with ground truths extracted
verbatim from official protocols; (iii) an empirical analysis of the
trade-off between Brazilian data-protection compliance and retrieval
performance; and (iv) a multidimensional evaluation protocol for
clinical RAG with small evaluator samples, combining RAGAS, a clinical
rubric, and a semi-structured interview organized along the TAM-AIN
dimensions.

\vspace{\onelineskip}
\noindent\textbf{Keywords:} latent tuberculosis infection; LTBI; clinical chatbot;
retrieval-augmented generation; RAG; large language models; nursing; data privacy.
\end{otherlanguage*}
\end{resumo}

% ─── Lista de figuras ────────────────────────────────────────────────────────
\pdfbookmark[0]{\listfigurename}{lof}
\listoffigures*
\cleardoublepage

% ─── Lista de quadros ────────────────────────────────────────────────────────
% O pacote newfloat (carregado no preâmbulo) cria automaticamente
% \listofquadros com o título "Lista de Quadros" (definido em listname).
% Usar \listof{quadro}{Lista de Quadros} é sintaxe do pacote `float` e
% gera o bug "quadroLista de Quadros" no PDF.
\pdfbookmark[0]{Lista de Quadros}{loq}
\listofquadros
\cleardoublepage

% ─── Lista de tabelas ────────────────────────────────────────────────────────
\pdfbookmark[0]{\listtablename}{lot}
\listoftables*
\cleardoublepage

% ─── Lista de abreviaturas e siglas ──────────────────────────────────────────
\begin{siglas}
  \item[ABNT]   Associação Brasileira de Normas Técnicas
  \item[ANN]    Approximate Nearest Neighbor (busca por vizinhos aproximados)
  \item[API]    Application Programming Interface
  \item[BERT]   Bidirectional Encoder Representations from Transformers
  \item[CEP]    Comitê de Ética em Pesquisa
  \item[DSRM]   \textit{Design Science Research Methodology}
  \item[HNSW]   Hierarchical Navigable Small World
  \item[IGRA]   Interferon-Gamma Release Assay
  \item[ILTB]   Infecção Latente por \textit{Mycobacterium tuberculosis}
  \item[LLM]    \textit{Large Language Model} (grande modelo de linguagem)
  \item[LGPD]   Lei Geral de Proteção de Dados Pessoais
  \item[MS]     Ministério da Saúde
  \item[NLP]    \textit{Natural Language Processing} (processamento de linguagem natural)
  \item[OMS]    Organização Mundial da Saúde
  \item[PPD]    Derivado Proteico Purificado (Teste Tuberculínico)
  \item[PT]     Prova Tuberculínica
  \item[PVHIV]  Pessoas Vivendo com HIV
  \item[RAG]    \textit{Retrieval-Augmented Generation}
  \item[RAGAS]  \textit{Retrieval Augmented Generation Assessment}
  \item[REST]   \textit{Representational State Transfer}
  \item[SBERT]  Sentence-BERT
  \item[SUS]    \textit{System Usability Scale}
  \item[TB]     Tuberculose
  \item[UFES]   Universidade Federal do Espírito Santo
\end{siglas}

% ─── Sumário ─────────────────────────────────────────────────────────────────
\pdfbookmark[0]{\contentsname}{toc}
\tableofcontents*
\cleardoublepage

% ═════════════════════════════════════════════════════════════════════════════
% ELEMENTOS TEXTUAIS
% ═════════════════════════════════════════════════════════════════════════════
\textual

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Introdução}
\label{cap:introducao}
% ─────────────────────────────────────────────────────────────────────────────

A tuberculose (TB) permanece como um dos maiores desafios de saúde pública no
mundo. Em 2022, a Organização Mundial da Saúde estimou 10,6 milhões de novos casos
de TB ativa, com o Brasil figurando entre os 30 países com maior carga da doença
\cite{who2023}. A forma latente da infecção --- denominada ILTB --- caracteriza-se
pela presença de \textit{Mycobacterium tuberculosis} no organismo sem manifestação
de doença ativa, mas com risco de progressão para TB ativa ao longo da vida,
sobretudo em indivíduos imunossuprimidos \cite{who2018}. O controle da ILTB é,
portanto, estratégico para a eliminação da TB como problema de saúde pública.

Nesse cenário, profissionais de enfermagem desempenham papel central no rastreamento,
na testagem e no acompanhamento de pacientes com ILTB. Contudo, os protocolos
clínicos são extensos, frequentemente atualizados e nem sempre de fácil consulta
durante o atendimento. Ferramentas de apoio à decisão clínica baseadas em
inteligência artificial (IA) surgem como alternativa promissora para reduzir essa
lacuna \cite{SHTI2023}.

Os grandes modelos de linguagem (\textit{Large Language Models} --- LLMs)
demonstraram capacidade notável de compreensão e geração de texto em domínios
especializados \cite{brown2020}. No entanto, modelos sem acesso a bases de
conhecimento atualizadas tendem a produzir respostas incorretas ou fabricadas ---
fenômeno conhecido como alucinação \cite{bang2023}. A arquitetura RAG
(\textit{Retrieval-Augmented Generation}) mitiga esse problema ao recuperar trechos
relevantes de documentos específicos antes de gerar a resposta \cite{lewis2021,
gao2024}.

\section{Problema e Motivação}

Apesar do avanço dos LLMs e da arquitetura RAG, três lacunas convergem para
caracterizar o problema enfrentado neste trabalho. A primeira é \textbf{linguística e
de domínio}: poucos trabalhos abordam aplicações de RAG em português do Brasil para
doenças infecciosas, e benchmarks como o PROPOR/ENAMED mostram que LLMs apresentam
desempenho inferior em consultas ao protocolo clínico brasileiro quando comparados
ao desempenho em corpora em inglês \cite{teachingLLMsBrazil2026,
adaptingLLMsPortuguese2024, llmsMedicalExam2026}.

A segunda é \textbf{regulatória e ética}: a Lei Geral de Proteção de Dados Pessoais
(LGPD) classifica dados de saúde como sensíveis e impõe restrições ao tratamento por
operadores terceirizados, o que problematiza arquiteturas RAG que dependem de APIs
externas para embeddings ou inferência \cite{lgpdSaude2023, lgpdEnfermagem2022,
sokPrivacyLLM2026, privacyRAGHealthcare2025}. Soluções voltadas à atenção primária do
SUS precisam reconciliar capacidade clínica com conformidade legal.

A terceira é \textbf{operacional}: a adoção de ferramentas de apoio à decisão
clínica por profissionais de enfermagem está condicionada a fatores de utilidade
percebida, alinhamento ético e prontidão organizacional --- aspectos modelados pelo
TAM-AIN (\textit{Technology Acceptance Model for AI in Nursing})
\cite{nursingTAM2024}. A ausência de instrumentação que cubra simultaneamente
acurácia técnica e essas dimensões de adoção é uma lacuna metodológica relevante
para o cenário brasileiro.

Este trabalho atende essas três lacunas ao desenvolver e avaliar um artefato RAG
para ILTB com corpus em português, pipeline de embeddings local (sem envio de
consultas a terceiros antes da busca) e protocolo de avaliação que combina métricas
RAGAS automáticas, rubrica clínica multidimensional e entrevista
semi-estruturada inspirada nas dimensões do TAM-AIN.

\section{Objetivo Geral}

Desenvolver e avaliar um chatbot de apoio clínico ao manejo da ILTB baseado na
arquitetura RAG, com corpus em língua portuguesa, respeitando boas práticas de
privacidade de dados e avaliado com métricas objetivas de qualidade de resposta.

\section{Objetivos Específicos}

\begin{enumerate}[label=\textbf{OE\arabic*}]
  \item Construir e indexar um corpus de documentos clínicos sobre ILTB em
        português, provenientes de fontes oficiais;
  \item Implementar um pipeline RAG com embeddings multilíngues e LLM integrado
        via API;
  \item Avaliar o sistema com o framework RAGAS em termos de
        \textit{faithfulness}, \textit{answer relevancy}, \textit{context precision}
        e \textit{context recall};
  \item Conduzir avaliação qualitativa com especialista em ILTB para verificar
        utilidade clínica e aderência aos protocolos vigentes.
\end{enumerate}

\section{Contribuições}

As contribuições deste trabalho são:

\begin{enumerate}[label=\textbf{C\arabic*}]
  \item \textbf{Artefato RAG aberto para ILTB em português:} pipeline RAG
        funcional, indexando documentos oficiais do MS e da OMS, com código-fonte
        disponível para reprodução e adaptação.
  \item \textbf{Dataset de avaliação clínica:} 38 questões clínicas em português
        sobre ILTB, organizadas em seis categorias (esquemas terapêuticos,
        monitoramento, populações especiais, diagnóstico, interações medicamentosas
        e efeitos adversos), com \textit{ground truths} extraídos literalmente dos
        protocolos indexados, utilizáveis como \textit{benchmark} por trabalhos
        subsequentes em RAG clínico em PT-BR.
  \item \textbf{Análise empírica do \textit{trade-off} LGPD vs. desempenho:}
        documentação das escolhas arquiteturais que tornam o desenho do
        artefato \emph{compatível} com requisitos da LGPD no recorte de POC
        acadêmica (corpus público, \emph{embeddings} locais, retenção zero),
        e discussão das mitigações ativas adicionais necessárias para
        \emph{conformidade plena} em implantação institucional (RIPD/DPIA,
        DPA, \emph{scrubbing}, \emph{logging} auditável).
  \item \textbf{Protocolo de avaliação multidimensional para RAG clínico com
        $N=1$:} adaptação metodológica que combina avaliação RAGAS automatizada,
        rubrica clínica multidimensional e entrevista semi-estruturada
        organizada pelas dimensões do TAM-AIN, oferecendo alternativa a
        instrumentos inadequados a amostra reduzida (e.g., Kappa de Cohen,
        ou o próprio TAM-AIN como questionário pontuável).
\end{enumerate}

\section{Estrutura da Monografia}

Esta monografia está organizada da seguinte forma. O
Capítulo~\ref{cap:referencial} apresenta o referencial teórico. O
Capítulo~\ref{cap:metodologia} descreve a metodologia DSRM adotada. O
Capítulo~\ref{cap:desenvolvimento} detalha o desenvolvimento do artefato. O
Capítulo~\ref{cap:avaliacao} apresenta a avaliação e os resultados. O
Capítulo~\ref{cap:discussao} discute as implicações dos resultados. O
Capítulo~\ref{cap:conclusao} conclui o trabalho com contribuições e trabalhos
futuros.

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Referencial Teórico}
\label{cap:referencial}
% ─────────────────────────────────────────────────────────────────────────────

\section{Tuberculose e Infecção Latente (ILTB)}

A ILTB é definida pela OMS como um estado de resposta imune persistente à
estimulação por antígenos de \textit{Mycobacterium tuberculosis}, sem evidência de
TB ativa clinicamente manifesta \cite{who2018}. Estima-se que aproximadamente um
quarto da população mundial tenha sido infectada, com risco de progressão para TB
ativa de 5--10\% ao longo da vida em imunocompetentes \cite{who2023}. No Brasil, o
Ministério da Saúde define os critérios de rastreamento, diagnóstico e tratamento
por meio de seu Manual de Recomendações \cite{brasil2022}.

Determinados grupos apresentam risco aumentado de progressão para TB ativa e
constituem público prioritário para rastreamento e tratamento da ILTB. Entre eles
destacam-se pessoas vivendo com HIV (PVHIV), receptores de transplante de órgãos
sólidos ou medula, pacientes em uso de imunobiológicos (especialmente
anti-TNF$\alpha$), contactantes domiciliares de casos bacilíferos, profissionais de
saúde com exposição ocupacional, pacientes em diálise e gestantes em populações de
alta prevalência \cite{who2018, brasil2022}.

O Brasil adota três esquemas terapêuticos prioritários para a ILTB, conforme o
Manual de Recomendações do Ministério da Saúde \cite{brasil2022}: isoniazida diária
por seis ou nove meses (6H ou 9H), rifampicina diária por quatro meses (4R) e
rifapentina + isoniazida semanal por três meses (3HP). A escolha entre esquemas
considera idade, gravidez, função hepática, interações medicamentosas (notadamente
com antirretrovirais em PVHIV) e tolerabilidade, e a literatura clínica
internacional reconhece o 3HP como avanço operacional importante por reduzir o
tempo total de tratamento e melhorar adesão \cite{who2018, who2020consolidatedTB,
who2023}.

A enfermagem desempenha papel central no manejo programático da ILTB no SUS, atuando
no rastreamento ativo de contactantes, na realização da prova tuberculínica (PT) e
do \textit{Interferon-Gamma Release Assay} (IGRA), no acompanhamento mensal de
adesão e na detecção precoce de reações adversas. Esse papel é reconhecido nas
diretrizes da OMS, que identificam o desenvolvimento de recursos humanos e a entrega
de serviços na atenção primária como ações estratégicas para ampliar o tratamento da
ILTB \cite{who2018, who2020consolidatedTB, who2023}.

Em escala local, \citeauthor{artigo_perfil} \cite{artigo_perfil} caracterizam o
perfil epidemiológico da TB no Espírito Santo entre 2015 e 2020: foram
notificados 8.687 casos no estado no período, com predominância do sexo
masculino (71,06\%) e maior concentração de incidência na faixa etária de 29
a~39 anos. Os municípios da Região Metropolitana de Vitória apresentaram taxas
de incidência substancialmente superiores às do restante do estado, padrão
correlacionado a fatores como tabagismo, etilismo e densidade populacional. Esse
recorte local sustenta a relevância de ferramentas de apoio operacional para os
profissionais da atenção primária do SUS atuantes nesses municípios.

\section{Grandes Modelos de Linguagem (LLMs)}

Os LLMs são modelos de linguagem pré-treinados em corpora massivos, capazes de
realizar diversas tarefas de processamento de linguagem natural com poucos exemplos
\cite{brown2020}. A arquitetura Transformer permite capturar dependências de longo
alcance por meio do mecanismo de atenção.

Para o português, iniciativas como Sabiá-2 \cite{sabia2_2024} e estudos de
adaptação de modelos para o contexto médico brasileiro \cite{adaptingLLMsPortuguese2024,
teachingLLMsBrazil2026} avançaram o estado da arte. Avaliações em exames médicos
brasileiros demonstram desempenho variável entre os modelos disponíveis
\cite{llmsMedicalExam2026}.

Modelos open-weight --- com pesos publicamente liberados, ainda que sob
licenças que podem restringir uso comercial --- têm papel particular em contextos
regulatórios estritos. A família Llama, descrita por \citeauthor{dubey2024}
\cite{dubey2024}, exemplifica essa categoria: o release Llama~3 incluiu modelos de
8B a 405B parâmetros, treinados com dados multilíngues e otimizados para
\textit{Grouped Query Attention} (GQA), o que viabiliza inferência eficiente em
\textit{hardware} acessível. O Llama~3.3 70B, refresh subsequente da mesma família
adotado neste trabalho, mantém a capacidade de raciocínio do 405B com fração do
custo computacional, segundo a documentação técnica da Meta.

A relevância prática do paradigma open-weight para projetos de saúde em
contextos regulatórios estritos é dupla: \textbf{(i)} permite, em princípio, a
inferência local em servidor institucional, eliminando a transferência de prompts
clínicos a operadores externos --- caminho aderente à LGPD e à recomendação técnica
de evitar envio de dados sensíveis a APIs proprietárias \cite{lgpdSaude2023,
privacyRAGHealthcare2025}; \textbf{(ii)} desacopla a evolução da aplicação do
\textit{roadmap} comercial dos provedores fechados, reduzindo risco de
descontinuidade. No presente projeto, a inferência local não foi adotada por
limitação de \textit{hardware}; a alternativa escolhida foi acessar o Llama~3.3 70B
via API Groq, que não armazena prompts segundo sua política declarada. A discussão
das implicações dessa escolha para implantação institucional consta no
Capítulo~\ref{cap:discussao}.

\section{Retrieval-Augmented Generation (RAG)}

A arquitetura RAG \cite{lewis2021} combina um componente de recuperação de informação
com um LLM generativo. Em vez de depender apenas do conhecimento paramétrico do
modelo, o RAG recupera trechos de uma base de documentos externa e os inclui no
contexto da geração. Isso reduz alucinações e permite atualizar o corpus sem
retreinamento.

\citeauthor{gao2024} \cite{gao2024} categorizam variantes de RAG em básico,
avançado e modular. Estratégias de chunking influenciam diretamente a
qualidade da recuperação \cite{advancedChunkingRAG2025}. Técnicas de busca híbrida,
como RRF \cite{cormack2009}, combinam recuperação esparsa (BM25, SPLADE
\cite{formal2021}) com recuperação densa para melhorar o \textit{recall}.

No domínio de saúde, aplicações RAG precisam considerar privacidade e conformidade
regulatória \cite{privacyRAGHealthcare2025, privacyEHRLLMs2025}.

\section{Embeddings e Busca Semântica}

Embeddings são representações vetoriais densas que capturam significado semântico
de textos \cite{boykis2024}. Modelos como Sentence-BERT \cite{reimers2019} permitem
calcular similaridade semântica com eficiência. A busca por vizinhos aproximados
(ANN) com índice HNSW \cite{malkov2016} viabiliza a recuperação em grandes bases.

Para o português clínico, o BioBERTpt \cite{biobertpt2020} foi um dos primeiros
modelos especializados em NLP biomédico, com bons resultados em reconhecimento de
entidades clínicas \cite{clinicalNERPortuguese2026}. O benchmark MMTEB
\cite{mmteb2025} e estudos sobre embeddings para busca clínica
\cite{generalistEmbeddingsClinical2024} fornecem panorama comparativo dos modelos
disponíveis.

Neste trabalho adotou-se o modelo \texttt{paraphrase-multilingual-MiniLM-L12-v2}
(384 dimensões), descendente distilado da família SBERT \cite{reimers2019} treinado
para similaridade semântica multilíngue em mais de 50 idiomas, incluindo o português.
A escolha foi orientada por três critérios: (i) execução local sem dependência de APIs
externas, requisito da política de privacidade do projeto (Seção~\ref{sec:lgpd-ref});
(ii) baixo \textit{footprint} de memória e latência de inferência compatíveis com a
infraestrutura disponível; e (iii) ampla adoção do modelo em sistemas RAG biomédicos
recentes \cite{ocaf2024}.

A literatura, contudo, revela uma tensão entre conveniência operacional e desempenho de
recuperação. Avaliações em corpora hospitalares mostram que modelos de 1024 dimensões,
como \texttt{multilingual-e5-large} e \texttt{BGE-M3}, atingem acurácia
\textit{Top}-10 superior a 90\%, enquanto variantes da família MiniLM permanecem abaixo
de 40\% nos mesmos cenários \cite{SHTI2023, mmteb2025}. Para o português clínico especificamente, o benchmark SemClinBr
\cite{clinicalNERPortuguese2026} indica que o mmBERT
\cite{marone2025mmbert} supera BioBERTpt e BERTimbau em reconhecimento
de entidades --- achado consistente com a tendência, em domínios
técnicos especializados, de \textit{embeddings} generalistas superarem
modelos de adaptação de domínio estreita
\cite{generalistEmbeddingsClinical2024}. Esses achados respaldam a estratégia de
empregar um modelo generalista multilíngue e motivaram a migração da configuração
inicial para um modelo 1024D (\texttt{BAAI/bge-m3}) durante a fase de avaliação
deste trabalho --- decisão documentada no Capítulo~\ref{cap:avaliacao}
(Seção~\ref{sec:gate-pos-migracao}), com ganho de $+47{,}8\%$ em
\textit{Faithfulness} sobre a configuração 384D.

\section{Avaliação de Sistemas RAG}

O framework RAGAS \cite{es2024} propõe métricas automáticas para avaliar
sistemas RAG sem anotação humana extensiva:

\begin{itemize}
  \item \textbf{\textit{Faithfulness}}: proporção de afirmações na resposta
        suportadas pelo contexto recuperado;
  \item \textbf{\textit{Answer Relevancy}}: relevância da resposta à pergunta
        original;
  \item \textbf{\textit{Context Precision}}: proporção de trechos recuperados
        úteis para a resposta;
  \item \textbf{\textit{Context Recall}}: cobertura do \textit{ground-truth}
        pelo contexto recuperado.
\end{itemize}

O RAGAS apresenta limitações conhecidas que devem ser ponderadas na interpretação dos
resultados. Em primeiro lugar, todas as métricas dependem de um LLM-juiz para decompor
respostas em afirmações atômicas e verificar suporte no contexto, o que introduz
variabilidade entre execuções e sensibilidade ao modelo avaliador escolhido
\cite{es2024}. Em segundo lugar, a métrica de \textit{Faithfulness} privilegia
respostas extrativas: sínteses parafraseadas, ainda que clinicamente corretas, tendem a
ser penalizadas porque o juiz não consegue mapear cada afirmação literalmente ao
contexto recuperado \cite{bang2023}.

A literatura sobre alucinação em LLMs clínicos diferencia categorias relevantes para a
análise diagnóstica desses scores: alucinações \textit{intrínsecas} (saída contradiz a
fonte), alucinações \textit{extrínsecas} (saída não verificável a partir da fonte) e
alucinações de \textit{citação} (referências inventadas) \cite{ocaf2024}. Cada classe
exige instrumentos de medida distintos, e revisões sistemáticas recentes recomendam
tratar a acurácia de citação como métrica separada da fidelidade ao contexto, sob pena
de subestimar erros graves de proveniência \cite{privacyRAGHealthcare2025}. Por essas
razões, este trabalho complementa a avaliação automática RAGAS com uma sessão
estruturada de avaliação humana com especialista (Capítulo~\ref{cap:avaliacao}),
seguindo a recomendação de combinar métricas automáticas e humanas em RAG biomédico
\cite{ocaf2024}.

\section{Privacidade de Dados e LGPD}
\label{sec:lgpd-ref}

A LGPD (Lei n\textordmasculine\ 13.709/2018) classifica dados de saúde como sensíveis,
sujeitos a requisitos mais rigorosos de consentimento e segurança \cite{lgpdSaude2023,
lgpdEnfermagem2022}. No contexto de LLMs, riscos de vazamento por memorização de
dados de treinamento ou \textit{prompt injection} são documentados
\cite{sokPrivacyLLM2026, privacyEHRLLMs2025}. A literatura específica sobre RAG em
saúde aprofunda esse cenário, identificando que prompts clínicos transmitidos a APIs
de terceiros configuram texto simples no nó de serviço, vulneráveis a inversão de
caixa-preta a partir de ativações intermediárias e à retenção em logs de longo prazo
\cite{privacyRAGHealthcare2025}.

A enfermagem está submetida a obrigações específicas de sigilo profissional. A
Resolução COFEN n\textordmasculine\ 564/2017 (Código de Ética) e a Resolução COFEN
n\textordmasculine\ 429/2012 (registros em prontuário) impõem dever de cautela no
manuseio de dados sensíveis de pacientes, e o descumprimento da LGPD pode ensejar
responsabilização ética e civil \cite{lgpdEnfermagem2022}. No plano regulatório
brasileiro mais amplo, a Autoridade Nacional de Proteção de Dados (Resolução ANPD
n\textordmasculine\ 4/2023) e a Estratégia de Saúde Digital 2020--2028 do Ministério
da Saúde, instituída pelo Decreto n\textordmasculine\ 11.358/2023, balizam o
tratamento de dados em sistemas digitais de saúde \cite{lgpdSaude2023}.

A literatura técnica documenta um conjunto de técnicas para mitigação dos riscos de
vazamento via prompt: \textit{Privacy Span Removal} (substituição seletiva de trechos
sensíveis), \textit{Instance Obfuscation} (\textit{hashing} dinâmico por sessão),
\textit{scrubbing} assistido por modelos de desidentificação e mecanismos de
\textit{Adaptive Differential Privacy} aplicados em nível de span
\cite{sokPrivacyLLM2026, privacyRAGHealthcare2025}. Tais técnicas tornam-se
indispensáveis quando o pipeline envia queries clínicas a serviços externos.

No presente projeto, optou-se por uma estratégia conservadora que dispensa essas
mitigações em tempo de execução: (i) o corpus é composto exclusivamente por documentos
públicos do Ministério da Saúde e da OMS, sem dados de paciente; (ii) o pipeline de
embeddings executa-se 100\% local, de modo que queries clínicas não transitam por APIs
de terceiros antes da busca vetorial; (iii) o LLM é acessado via API Groq apenas para
geração da resposta final, sem armazenamento de conversas pelo provedor; e (iv)
nenhuma informação identificável de paciente é inserida nas perguntas de avaliação.
Para a sessão estruturada com o especialista (Capítulo~\ref{cap:avaliacao}), serão
adotados Termo de Consentimento Livre e Esclarecido, política de retenção zero do
histórico de conversa e \textit{logging} mínimo, conforme recomendado pela literatura
para provas de conceito acadêmicas em saúde \cite{privacyRAGHealthcare2025}.

\section{Adoção de IA por Profissionais de Saúde}
\label{sec:adocao-ref}

O Modelo de Aceitação de Tecnologia (TAM) é amplamente usado para explicar a adoção
de sistemas de TI por profissionais de saúde \cite{nursingTAM2024}. Fatores como
utilidade percebida, facilidade de uso e confiança no sistema são determinantes
para a adoção de chatbots clínicos por enfermeiros.

Estudos recentes propõem o TAM-AIN (\textit{Technology Acceptance Model for AI in
Nursing}) como extensão específica para a categoria, incorporando quatro dimensões
ausentes do TAM clássico: (i) \textit{alinhamento ético}, que avalia a compatibilidade
da ferramenta com os valores da profissão e o sigilo do paciente; (ii) \textit{prontidão
organizacional}, que examina o apoio da liderança e a infraestrutura local;
(iii) \textit{preservação da identidade profissional}, relacionada à percepção de
ameaça ou reforço à autonomia clínica e ao componente humano do cuidado; e
(iv) \textit{capacidade de infraestrutura técnica}, que considera as restrições
materiais do ambiente assistencial \cite{nursingTAM2024}. Essa especialização é
particularmente relevante para o SUS, onde a heterogeneidade de infraestrutura entre
unidades de atenção primária pode invalidar premissas de modelos genéricos derivados
de contextos hospitalares de alta complexidade.

A literatura sobre percepção de enfermeiros frente à IA indica padrão consistente de
``otimismo cauteloso'': cerca de 85\% dos profissionais reconhecem benefícios para a
precisão diagnóstica e a redução de erros, mas aproximadamente 70\% mantêm reservas
quanto à preservação do componente humano do cuidado e à segurança dos dados
\cite{nursingTAM2024}. As barreiras mais reportadas incluem infraestrutura tecnológica
obsoleta, sobrecarga de trabalho, ceticismo sobre a capacidade da IA de replicar o
julgamento clínico e, sobretudo entre profissionais com mais tempo de prática, receio
de desqualificação. Esses achados orientam a estrutura da sessão de avaliação com o
especialista (Capítulo~\ref{cap:avaliacao}), que combina rubrica clínica
multidimensional com uma entrevista semi-estruturada organizada pelas dimensões
do TAM-AIN para capturar tanto a acurácia técnica quanto as dimensões éticas e
organizacionais da aceitação (a adaptação a $N=1$ está descrita na
Seção~\ref{sec:sessao-expert}).

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Metodologia}
\label{cap:metodologia}
% ─────────────────────────────────────────────────────────────────────────────

Este trabalho adota a \textit{Design Science Research Methodology} (DSRM)
\cite{peffers2007}, metodologia consagrada para pesquisa em sistemas de informação
orientada à construção de artefatos. O DSRM estrutura a pesquisa em seis atividades
iterativas, conforme a Figura~\ref{fig:dsrm}.

\begin{figure}[!htbp]
  \centering
  \caption{Processo DSRM aplicado ao desenvolvimento do chatbot ILTB}
  \label{fig:dsrm}
  \vspace{0.3em}
  \begin{tikzpicture}[
    node distance=0.6cm and 0.7cm,
    every node/.style={font=\footnotesize},
    stage/.style={
      rectangle, rounded corners=4pt, draw=black!75, thick,
      fill=blue!5, minimum width=2.6cm, minimum height=1.1cm,
      align=center, inner sep=4pt,
    },
    output/.style={
      rectangle, draw=black!50, dashed, fill=gray!5,
      minimum width=2.6cm, minimum height=0.7cm,
      align=center, inner sep=3pt, font=\scriptsize\itshape,
    },
    arrow/.style={-{Latex[length=2.2mm]}, thick, black!70},
    feedback/.style={-{Latex[length=2.2mm]}, thick, black!50, dashed},
  ]
    % Linha 1: etapas 1, 2, 3
    \node[stage] (s1) {1. Identificação\\do Problema\\e Motivação};
    \node[stage, right=of s1] (s2) {2. Definição\\dos Objetivos\\da Solução};
    \node[stage, right=of s2] (s3) {3. Design e\\Desenvolvimento\\do Artefato};

    % Linha 2: etapas 6, 5, 4 (invertida para retorno em U)
    \node[stage, below=1.4cm of s3] (s4) {4. Demonstração\\do Artefato};
    \node[stage, left=of s4] (s5) {5. Avaliação\\(RAGAS + Expert)};
    \node[stage, left=of s5] (s6) {6. Comunicação\\(Monografia +\\Repositório)};

    % Setas principais (fluxo)
    \draw[arrow] (s1) -- (s2);
    \draw[arrow] (s2) -- (s3);
    \draw[arrow] (s3) -- (s4);
    \draw[arrow] (s4) -- (s5);
    \draw[arrow] (s5) -- (s6);

    % Iteração: avaliação volta para design
    \draw[feedback]
      (s5.north) ++(0,0.15)
      -- ++(0,0.4)
      -| node[pos=0.25, above, font=\scriptsize\itshape] {iteração (C3--C5)}
      ($(s3.south) + (0.6,0)$);

    % Iteração menor: avaliação refina objetivos (lições do paradoxo)
    \draw[feedback]
      (s5.north west) ++(-0.05,0.05)
      -- ++(0,0.9)
      -| ($(s2.south) + (-0.4,0)$);
  \end{tikzpicture}
  \caption*{Fonte: Adaptado de \citeauthor{peffers2007} (\citeyear{peffers2007}).
            Setas sólidas indicam o fluxo principal; setas tracejadas
            indicam ciclos de iteração observados neste trabalho
            (Quadro~\ref{quad:ciclos-dsrm}).}
\end{figure}

O Quadro~\ref{quad:ciclos-dsrm} sintetiza os ciclos iterativos executados no
projeto, mapeando hipótese testada, intervenção, resultado mensurável e
lição aprendida que orientou o ciclo subsequente. A organização em ciclos
explicita o caráter iterativo da DSRM e fornece rastreabilidade entre
intervenções técnicas (Capítulo~\ref{cap:desenvolvimento}) e resultados
empíricos (Capítulo~\ref{cap:avaliacao}).

\begin{quadro}[!htbp]
  \centering
  \small
  \renewcommand{\arraystretch}{1.15}
  \caption{Ciclos DSRM observados no desenvolvimento do artefato}
  \label{quad:ciclos-dsrm}
  \begin{tabularx}{\textwidth}{|c|X|X|X|}
  \hline
  \rowcolor{gray!25}
  \textbf{Ciclo} & \textbf{Hipótese / intervenção} & \textbf{Resultado mensurável} & \textbf{Lição $\to$ próximo ciclo} \\ \hline
  C1 & Sanitização de \textit{chunks} e estabilização do \textit{system prompt}
       resolvem o problema de extração ruidosa do PDF. &
       \textit{Faithfulness} de $0{,}375$ para $0{,}586$ ($+56\%$);
       \textit{Answer Relevancy} dobra ($0{,}310 \to 0{,}534$). &
       Pré-processamento era gargalo, mas o teto da iteração ainda não
       atinge a meta. Indica gargalo seguinte em recuperação ou geração. \\ \hline
  C2 & \textit{Chunker} hierárquico por cabeçalhos Markdown e
       $k=5$ estabilizam o \textit{retrieval}. &
       \textit{Context Precision} $0{,}735$ (próximo da meta $0{,}75$),
       mas \textit{Faithfulness} estagna em $0{,}515$. &
       \textit{Retrieval} convergiu; o gargalo é gerador ou
       \textit{embedding}, hipótese a ser separada via ablação cruzada. \\ \hline
  C3 & Ablação $2 \times 2$ isola gerador
       (Llama \emph{vs} GPT-4o) $\times$ \textit{embedding} (MiniLM 384D
       \emph{vs} BGE-M3 1024D) sobre 3 seeds por célula. &
       \textit{Embedding} é variável dominante: BGE-M3 eleva
       \textit{Faithfulness} de $0{,}486$ a $0{,}679$ no gerador Llama
       ($+39{,}7\%$); GPT-4o piora \textit{Faithfulness} em ambos os
       \textit{embeddings} (paradoxo abstrativo--vs--conservador). &
       Migração para BGE-M3 1024D no \textit{pipeline} de produção;
       adoção do Llama como gerador validada pelos próprios números;
       interpretação cautelosa do \textit{Faithfulness} como métrica. \\ \hline
  C4 & Busca híbrida BM25 $+$ denso com RRF endereça termos clínicos
       exatos (siglas, dosagens) não capturados pelo BGE-M3. &
       \textit{Faithfulness} $0{,}679 \to 0{,}740$ ($+9{,}0\%$, $\Delta$
       $\approx 3{,}6\times$ DP); \textit{Context Recall} $+8{,}5\%$. &
       Híbrido é motor dominante de \textit{Faithfulness}; resta consolidar
       \textit{Context Precision} e reduzir variância inter-seed. \\ \hline
  C5 & \textit{Reranker} \textit{cross-encoder} mGTE reordena os
       candidatos do RRF antes da geração. &
       \textit{Context Precision} $0{,}915 \to 0{,}956$ ($+0{,}041$); DP
       inter-seed da \textit{Faithfulness} cai de $\pm 0{,}017$ para
       $\pm 0{,}004$. &
       \textit{Reranker} consolida Ctx.Prec e estabiliza
       \textit{Faithfulness}. Gate operacional final do artefato. \\ \hline
  C6 & Avaliação com especialista (rubrica clínica multidimensional $+$
       entrevista pelas dimensões do TAM-AIN). &
       \aluno{Sumarizar acurácia clínica média, severidade modal dos
       erros e síntese narrativa por dimensão TAM-AIN após execução.} &
       \aluno{Lição da sessão $\to$ refinamento do \textit{prompt} ou
       \textit{Trabalhos Futuros}.} \\ \hline
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor. Os ciclos C1 e C2 referem-se à
            Fase 2 do projeto (Tabela~\ref{tab:ragas}); C3 a C5 são as
            iterações pós-replicação multi-seed
            (Tabelas~\ref{tab:ablacao-2x2} e \ref{tab:hybrid-rerank}); C6 é
            a etapa de avaliação humana (Seção~\ref{sec:sessao-expert}).}
\end{quadro}

\section{Identificação do Problema e Motivação}

O problema central identificado é a dificuldade de acesso rápido a informações
clínicas padronizadas sobre ILTB por profissionais de enfermagem durante o
atendimento. A solução proposta é um chatbot de apoio à decisão clínica baseado em
RAG.

A identificação do problema partiu de duas fontes complementares.

\textbf{Análise documental} dos protocolos vigentes evidenciou que o Manual de
Recomendações do Ministério da Saúde \cite{brasil2022} é extenso, denso em tabelas
e atualizado periodicamente, dificultando a consulta rápida durante o
atendimento. As diretrizes da OMS
\cite{who2018, who2020consolidatedTB, who2023} complementam esse material e
contêm recomendações ocasionalmente desatualizadas em relação às diretrizes
nacionais, exigindo do profissional discernimento sobre qual fonte prevalece;
notadamente, o \textit{Module 1} de \citeyear{who2020consolidatedTB} consolida
os regimes preventivos curtos (3HP, 4R) que estão na fronteira de
recomendação no relatório global de 2023.

\textbf{Revisão da literatura} sobre RAG biomédico \cite{ocaf2024, lewis2021,
gao2024} e sobre adoção de IA por profissionais de enfermagem \cite{nursingTAM2024}
confirmou que ferramentas de apoio à decisão clínica em domínio específico ainda
são pouco exploradas para o português brasileiro, configurando lacuna acadêmica
relevante.

A partir dessas duas fontes formularam-se as duas proposições falsificáveis
que organizam a investigação empírica deste trabalho.

\textbf{Tese 1 (técnica).} \emph{No domínio ILTB em português, sobre corpus
oficial do MS e da OMS, a escolha do modelo de \emph{embedding} é variável
dominante de qualidade de recuperação: a substituição de um \emph{embedding}
multilíngue 384D (\texttt{MiniLM}) por um modelo 1024D
(\texttt{BAAI/bge-m3}) eleva a \emph{Context Precision} acima de
$0{,}90$ --- patamar inatingível pela MiniLM independentemente do modelo
gerador adotado.} Essa proposição é diretamente testada pela ablação
$2 \times 2$ apresentada na Seção~\ref{sec:ablacao-2x2}: a falsificação
ocorreria se o BGE-M3 não produzisse ganho mensurável em \textit{Context
Precision} sobre a MiniLM, ou se a MiniLM atingisse $\geq 0{,}90$.

\textbf{Tese 2 (avaliativa).} \emph{A métrica de \emph{Faithfulness} do
\emph{framework} RAGAS, na sua formulação atual, é assimétrica em relação ao
\emph{trade-off} segurança--cobertura: penaliza geradores que se abstêm
quando o contexto é parcial, mesmo quando a abstenção é clinicamente
preferível.} Essa proposição é testada pela comparação cruzada
gerador~$\times$~\emph{embedding} da mesma ablação: a falsificação ocorreria
se a substituição do Llama por um modelo mais aderente a instruções de
\textit{fallback} (GPT-4o) preservasse ou elevasse a \emph{Faithfulness} ---
o oposto do que se observou.

A confirmação ou refutação das duas teses orienta, em conjunto, a definição
do gate operacional do artefato e a escolha do indicador-síntese para o
trabalho (\emph{Context Precision} sobre \emph{Faithfulness}), conforme
desenvolvido no Capítulo~\ref{cap:avaliacao}.

\section{Definição dos Objetivos da Solução}

Os objetivos de design foram definidos com base nos requisitos funcionais e
não funcionais do sistema, derivados da análise do problema. Os
Quadros~\ref{quad:decisoes-tecnicas} e~\ref{quad:decisoes-metodologicas}
sintetizam as dez decisões (D1--D10) que admitiam alternativas reais e
exigiram justificativa bibliográfica explícita; o primeiro reúne decisões
\emph{técnico-arquiteturais} (D1--D6) e o segundo reúne decisões
\emph{metodológico-avaliativas} (D7--D10). \textbf{Três premissas
constitutivas do trabalho não figuram nos quadros}: a adoção da arquitetura
RAG, a adoção da metodologia DSRM e o foco em ILTB como problema clínico
--- todas declaradas no Capítulo~\ref{cap:introducao} e no início deste
capítulo, por serem o objeto da pesquisa e não escolhas discutíveis no
mesmo registro das demais. Decisões revisadas no decorrer do projeto ---
particularmente D7 e D9, em função do reescopo da etapa de avaliação, e D8,
antecipada ao escopo deste trabalho --- estão refletidas nos quadros em sua
versão final.

\begin{quadro}[!htbp]
  \centering
  \small
  \renewcommand{\arraystretch}{1.15}
  \caption{Decisões técnico-arquiteturais do chatbot ILTB (D1--D6)}
  \label{quad:decisoes-tecnicas}
  \begin{tabularx}{\textwidth}{|c|X|X|}
  \hline
  \rowcolor{gray!25}
  \textbf{ID} & \textbf{Decisão} & \textbf{Justificativa} \\ \hline
  D1  & Embedding multilíngue \texttt{paraphrase-multilingual-MiniLM-L12-v2}
        (384D) na fase inicial; migrado para \texttt{BAAI/bge-m3} (1024D) durante
        a avaliação (Seção~\ref{sec:gate-pos-migracao}). & Equilíbrio entre
        latência, \textit{footprint} e requisito LGPD; ganho de 1024D
        empiricamente observado \cite{reimers2019, mmteb2025, SHTI2023,
        marone2025mmbert}. \\ \hline
  D2  & Vector store ChromaDB com índice HNSW e persistência em disco.
      & Execução embarcada sem servidor dedicado; HNSW provê busca aproximada
        com complexidade logarítmica \cite{malkov2016, chroma2024}. \\ \hline
  D3  & LLM Llama~3.3~70B via API Groq como gerador. & Modelo open-weight
        competitivo sem retenção de prompts pelo provedor
        \cite{dubey2024}; modelos generalistas grandes superam especializados em
        busca clínica curta \cite{ocaf2024}. \\ \hline
  D4  & Chunking semântico por cabeçalhos Markdown, com
        \textit{buffering} de seções pequenas. & Preserva integridade lógica dos
        protocolos; alinhado a recomendações de chunking estrutural
        \cite{gao2024, advancedChunkingRAG2025}. \\ \hline
  D5  & Avaliação automatizada com RAGAS (\textit{faithfulness},
        \textit{answer relevancy}, \textit{context precision/recall})
        complementada por avaliação humana. &
        \textit{Framework reference-free} padrão em RAG biomédico, com
        limitações conhecidas que exigem complemento por especialista
        \cite{es2024, ocaf2024, gallifant2025tripod}. \\ \hline
  D6  & Pipeline de embeddings 100\% local; LLM acessado sem
        armazenamento de prompts. & Compatibilidade com requisitos
        da LGPD no recorte de POC (dados de saúde como sensíveis), evitando
        APIs externas para etapa de embedding; mitigações ativas
        (\emph{scrubbing}, DPA, RIPD) listadas como pré-requisito de
        implantação institucional \cite{lgpdSaude2023, lgpdEnfermagem2022,
        privacyRAGHealthcare2025, sokPrivacyLLM2026}. \\ \hline
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor.}
\end{quadro}

\begin{quadro}[!htbp]
  \centering
  \small
  \renewcommand{\arraystretch}{1.15}
  \caption{Decisões metodológico-avaliativas do chatbot ILTB (D7--D10)}
  \label{quad:decisoes-metodologicas}
  \begin{tabularx}{\textwidth}{|c|X|X|}
  \hline
  \rowcolor{gray!25}
  \textbf{ID} & \textbf{Decisão} & \textbf{Justificativa} \\ \hline
  D7  & Avaliação com pequeno painel de especialistas em ILTB
        (\textbf{$N \geq 1$}, prospecção ativa antes da defesa) usando
        rubrica clínica multidimensional e entrevista semi-estruturada
        pelas dimensões do TAM-AIN. & Replanejamento decorrente do
        reescopo do piloto original; as dimensões do TAM-AIN
        \cite{nursingTAM2024, davis1989tam} são adotadas como
        \emph{eixos de roteiro}, e os itens Likert pontuáveis são
        aplicados de forma descritiva apenas quando $N \geq 2$ ---
        análise fatorial e validade convergente do instrumento exigem
        amostra populacional, fora do escopo desta sessão exploratória
        \cite{ocaf2024}. \\ \hline
  D8  & Busca híbrida BM25 + denso (RRF) com reranking
        cross-encoder mGTE --- implementada no escopo deste trabalho
        (Seções~\ref{sec:hybrid} e \ref{sec:rerank}).
      & Embedding denso isolado limita \textit{recall} para termos
        técnicos específicos; literatura sustenta ganhos de busca híbrida
        \cite{cormack2009, formal2021, khattab2020, wang2024bestpractices,
        zhang2024mgte}. \\ \hline
  D9  & Métricas mistas (acurácia vs. MS, severidade Likert, tríade RAG,
        entrevista pelas dimensões do TAM-AIN), com aplicação dos itens
        Likert do TAM-AIN \emph{condicional ao $N$ de avaliadores
        disponíveis na sessão} (descritiva se $N \geq 2$; narrativa se
        $N=1$). Validade psicométrica completa (análise fatorial,
        $\alpha$ de Cronbach) e estatísticas de concordância (Kappa de
        Cohen) ficam para piloto multi-avaliador estruturado (TF6).
      & Tanto Kappa quanto a validade psicométrica do TAM-AIN exigem
        amostra populacional; literatura recomenda métricas
        multidimensionais e leitura descritiva para validação com $N$
        reduzido \cite{ocaf2024, es2024}. \\ \hline
  D10 & Documentação explícita da taxa de alucinação como propriedade conhecida
        do LLM, com ablação contra modelo de referência executada na fase de
        avaliação (Seção~\ref{sec:ablacao-2x2}). & Tipologia e taxas de
        alucinação reportadas em literatura sustentam o teto observado de
        \textit{faithfulness} no Llama~3.3~70B
        \cite{bang2023, ocaf2024, llmsMedicalExam2026,
        huang2023halucination}. \\ \hline
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor.}
\end{quadro}

\section{Design e Desenvolvimento}

O artefato é um sistema RAG composto pelos seguintes componentes principais:
\textbf{(a)} corpus de documentos clínicos públicos sobre ILTB em português;
\textbf{(b)} pipeline de ingestão (extração, chunking, embeddings);
\textbf{(c)} banco de vetores ChromaDB com índice HNSW;
\textbf{(d)} LLM Llama~3.3~70B via API Groq;
\textbf{(e)} API REST (FastAPI) e interface web de demonstração.

\section{Demonstração}

O artefato foi demonstrado via interface web de chat, servida pelo FastAPI e
acessível por meio de túnel ngrok. A demonstração incluiu 38 questões clínicas
sobre ILTB formuladas pelo autor com base nos documentos do corpus.

\section{Avaliação}

A avaliação foi estruturada em duas etapas complementares:

\begin{enumerate}[label=\textbf{(\arabic*)}]
  \item \textbf{Avaliação automatizada (RAGAS):} 38 questões clínicas com
        \textit{ground-truths} extraídos dos documentos indexados, usando
        \textit{faithfulness}, \textit{answer relevancy}, \textit{context
        precision} e \textit{context recall} \cite{es2024};
  \item \textbf{Avaliação qualitativa com especialista:} sessão estruturada com
        profissional especialista em ILTB, avaliando amostras de respostas do
        chatbot em termos de acurácia clínica e aderência aos protocolos vigentes.
\end{enumerate}

A descrição completa do protocolo da sessão com o especialista, incluindo perfil
do avaliador, etapas, instrumentos e rubrica multidimensional, é apresentada na
Seção~\ref{sec:sessao-expert} do Capítulo~\ref{cap:avaliacao}, evitando duplicação.

\section{Comunicação}

Os resultados são comunicados por meio desta monografia e disponibilizados em
repositório público acessível em \url{https://github.com/yago-novaes/chatbot-iltb}.

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Desenvolvimento do Artefato}
\label{cap:desenvolvimento}
% ─────────────────────────────────────────────────────────────────────────────

\section{Arquitetura do Sistema}

O chatbot ILTB é implementado como uma aplicação web com \textit{backend} em
Python (FastAPI) e banco de vetores ChromaDB \cite{chroma2024}. A
Figura~\ref{fig:arquitetura} apresenta a visão geral da arquitetura.

\begin{figure}[!htbp]
  \centering
  \caption{Arquitetura do chatbot ILTB baseado em RAG}
  \label{fig:arquitetura}
  \vspace{0.3em}
  \begin{tikzpicture}[
    every node/.style={font=\scriptsize, align=center},
    actor/.style={
      rectangle, rounded corners=2pt, draw=black!75, thick,
      fill=orange!10, minimum width=1.9cm, minimum height=0.7cm,
      inner sep=3pt,
    },
    component/.style={
      rectangle, rounded corners=2pt, draw=black!75, thick,
      fill=blue!8, minimum width=2.1cm, minimum height=0.7cm,
      inner sep=3pt,
    },
    storage/.style={
      cylinder, shape border rotate=90, draw=black!75, thick,
      fill=green!8, minimum width=1.8cm, minimum height=0.9cm,
      aspect=0.25, inner sep=2pt,
    },
    external/.style={
      rectangle, rounded corners=2pt, draw=black!75, thick, dashed,
      fill=red!6, minimum width=2.1cm, minimum height=0.7cm,
      inner sep=3pt,
    },
    flow/.style={-{Latex[length=1.8mm]}, thick, black!70},
    flowback/.style={-{Latex[length=1.8mm]}, thick, black!50},
    group/.style={
      rectangle, rounded corners=4pt, draw=black!30,
      dashed, inner sep=6pt,
    },
  ]
    % Coluna 1: usuário e front
    \node[actor] (user) {Enfermeiro\\(usuário)};
    \node[component, right=1.0cm of user] (web) {Interface Web\\(HTML/JS)};
    \node[component, right=1.0cm of web] (api) {FastAPI\\(\texttt{/chat})};

    % Coluna 2 (linha 2): pipeline RAG
    \node[component, below=1.3cm of api] (retr) {Retriever\\(hybrid + rerank)};
    \node[component, left=0.7cm of retr] (emb) {Embedding\\BGE-M3 1024D\\(local)};
    \node[storage, left=0.7cm of emb] (chroma) {ChromaDB\\898 chunks};
    \node[component, below=0.7cm of retr] (bm25) {BM25\\\textit{sidecar}};
    \node[component, right=0.7cm of retr] (rer) {Reranker\\mGTE (CPU)};

    % LLM externo
    \node[external, above=0.5cm of rer, xshift=1.4cm] (llm) {Llama 3.3 70B\\via API Groq\\(sem retenção)};

    % Setas
    \draw[flow] (user) -- node[above, font=\tiny] {pergunta} (web);
    \draw[flow] (web) -- (api);
    \draw[flow] (api) -- ++(0,-0.65) -| node[pos=0.25, above, font=\tiny] {\texttt{query}} (retr);
    \draw[flow] (retr) -- (emb);
    \draw[flow] (emb) -- (chroma);
    \draw[flow] (chroma.south) |- ([yshift=-0.4cm]bm25.west) -- (bm25.west);
    \draw[flow] (bm25.east) -- ([xshift=0.4cm]bm25.east) |- (rer.south);
    \draw[flow] (rer.north) -- ++(0,0.25) -| (llm.south);
    \draw[flowback] (llm.west) -| node[pos=0.75, right, font=\tiny] {resposta} (api.east);
    \draw[flowback] (api) -- node[above, font=\tiny] {resposta} (web);
    \draw[flowback] (web) -- (user);
  \end{tikzpicture}
  \caption*{Fonte: Produção do próprio autor. Componentes em azul: backend
            local. Cilindro verde: persistência (ChromaDB com índice HNSW).
            Caixa tracejada vermelha: serviço externo (LLM via Groq, sem
            armazenamento de \textit{prompts}). Setas sólidas pretas
            representam o fluxo da consulta; setas mais claras, o retorno
            da resposta.}
\end{figure}

\section{Corpus e Pré-processamento}

O corpus indexado é composto por seis documentos públicos sobre o manejo da
tuberculose e da ILTB, com total de 551~páginas, conforme detalhado na
Tabela~\ref{tab:corpus}. A seleção integra três fontes ministeriais brasileiras
(MS), uma da OPAS/OMS em português e uma de sociedade médica nacional (GEDIIB),
garantindo: (i) cobertura completa do protocolo brasileiro vigente; (ii)
alinhamento com recomendações internacionais; e (iii) domínio público ---
requisito para a estratégia de privacidade adotada (Seção~\ref{sec:lgpd-ref}). A
sustentação teórica do referencial clínico (Seção~\ref{cap:referencial}) apoia-se
adicionalmente nos documentos canônicos da OMS \cite{who2018, who2023} e no
\textit{Manual de Recomendações para o Controle da Tuberculose no Brasil}
\cite{brasil2022}.

\begin{table}[h]
  \centering
  \caption{Documentos indexados no corpus do chatbot ILTB}
  \label{tab:corpus}
  \begin{tabularx}{\textwidth}{@{}lXrr@{}}
  \toprule
  \textbf{Identificador} & \textbf{Título} & \textbf{Ano} & \textbf{Pág.} \\
  \midrule
  \textsc{ms-manual}       & Manual de Recomendações para o Controle da
                             Tuberculose no Brasil (MS, 2ª ed.\ atualizada) &
                             2019 & 366 \\
  \textsc{ms-guia}         & Recomendações para o Controle da Tuberculose ---
                             Guia Rápido para Profissionais de Saúde (MS, 2ª
                             ed.) & 2021 & 50 \\
  \textsc{ms-vig-iltb}     & Protocolo de Vigilância da Infecção Latente pelo
                             \textit{M.\,tuberculosis} no Brasil (MS, 2ª ed.) &
                             2022 & 38 \\
  \textsc{ms-3hp}          & Tratamento da Infecção Latente com Rifapentina +
                             Isoniazida (3HP) --- MS/SVS & 2021 & 2 \\
  \textsc{opas-mod4}       & Manual Operacional de Tuberculose da OMS ---
                             Módulo 4: Tratamento (OPAS/OMS, versão em
                             português) & 2020 & 84 \\
  \textsc{gediib-tb}       & Tratamento da Tuberculose Infecção: Posicionamentos
                             Científicos do GEDIIB & 2024 & 11 \\
  \midrule
  \multicolumn{3}{l}{\textbf{Total}} & \textbf{551} \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor.}
\end{table}

O pipeline de ingestão extrai texto e estrutura dos PDFs em duas etapas:
extração inicial com \texttt{pypdf}, seguida por sanitização e reorganização em
Markdown estruturado por cabeçalhos (\texttt{\#}~a~\texttt{\#\#\#\#}). Durante a
extração foram identificados desafios típicos de PDFs governamentais
brasileiros, com destaque para a desconfiguração de tabelas (esquemas
terapêuticos, doses por faixa etária) e uma falha de \textit{parsing} que produzia
\textit{gap} de 34 páginas no Manual do MS. Esse defeito foi diagnosticado e
corrigido por meio de \textit{patches} pontuais no \textit{stream} extraído,
documentados no diário técnico do projeto.

A sanitização Markdown padroniza unidades, doses e nomes de fármacos, removendo
artefatos de \textit{OCR} e normalizando hierarquia de seções. O produto final
é um conjunto de arquivos \texttt{.md} estruturados, prontos para a etapa de
chunking.

O conjunto totaliza 551~páginas (Tabela~\ref{tab:corpus}), e após a sanitização
e o chunking (Seção~\ref{cap:desenvolvimento}) é convertido em 898
chunks indexados, distribuídos majoritariamente entre o
\textsc{ms-manual} (que concentra a maior densidade de protocolo clínico) e o
\textsc{opas-mod4}.

\section{Segmentação (Chunking) e Indexação}

A estratégia de chunking adotada é hierárquica e dirigida por estrutura:
o documento é segmentado nas fronteiras de cabeçalho Markdown
(\texttt{\#} a \texttt{\#\#\#\#}), reproduzindo a hierarquia conceitual dos
protocolos. Essa abordagem alinha-se às recomendações de \citeauthor{gao2024}
\cite{gao2024} de privilegiar fronteiras semânticas naturais sobre janelas fixas,
e às evidências quantitativas reportadas para chunking estrutural em
domínios técnicos \cite{advancedChunkingRAG2025}.

Para evitar fragmentação excessiva de seções curtas, o \textit{chunker} aplica
\textit{buffering}: seções abaixo de um tamanho mínimo
(\texttt{MIN\_CHUNK\_SIZE}~$=400$ caracteres) são agregadas à seção subsequente do
mesmo nível hierárquico. Seções acima do limite superior são subdivididas por
parágrafos, preservando coerência sintática. A regex de detecção de cabeçalho é
\texttt{\textasciicircum\#\{1,4\}\textbackslash s} para capturar os quatro níveis
relevantes no corpus.

O processo de chunking foi iterado ao longo da Fase~2 do desenvolvimento,
com avaliações RAGAS sucessivas para calibrar o parâmetro \texttt{MIN\_CHUNK\_SIZE}.
A configuração final produziu \textbf{898 chunks} a partir dos três
documentos indexados, distribuídos majoritariamente entre o Manual do MS
\cite{brasil2022} e as diretrizes da OMS \cite{who2018}. Esses chunks
são persistidos no ChromaDB com seus respectivos vetores e metadados de origem
(arquivo, hierarquia de cabeçalhos, posição relativa).

\section{Modelo de Embeddings}

O modelo de embeddings adotado é o
\texttt{paraphrase-multilingual-MiniLM-L12-v2}, da família SBERT
\cite{reimers2019}, com 384 dimensões e suporte a mais de 50 idiomas. Foi carregado
via biblioteca \texttt{sentence-transformers} e executado localmente, sem requisição
a APIs externas, como descrito conceitualmente na
Seção~\ref{cap:referencial} e no item D1 do
Quadro~\ref{quad:decisoes-tecnicas}.

A escolha foi orientada por três critérios operacionais já discutidos: (i)
conformidade com a política LGPD do projeto (Seção~\ref{sec:lgpd-ref}); (ii)
\textit{footprint} compatível com a infraestrutura disponível; (iii) ampla
adoção em RAG biomédico \cite{ocaf2024}. O modelo gera vetores de 384 dimensões
que são indexados no ChromaDB com métrica de similaridade cossenoidal.

\textbf{Limitação observada empiricamente.} Um experimento conduzido durante o
desenvolvimento adicionou contexto hierárquico explícito a cada chunk
(prefixo com a cadeia de cabeçalhos), prática conhecida como
\textit{contextual chunking}. Essa modificação \textit{piorou} os resultados
RAGAS em todas as métricas, comportamento consistente com a literatura
\cite{SHTI2023}: modelos de baixa dimensionalidade (384D) misturam semântica
estrutural com semântica clínica no vetor resultante, degradando a precisão.
Modelos de maior dimensionalidade ($\geq 1024$D) absorvem melhor essa
informação adicional, o que reforça a indicação de migração para um modelo
1024D (e.g., \texttt{multilingual-e5-large}, \texttt{BGE-M3}) como trabalho
futuro prioritário (Capítulo~\ref{cap:conclusao}).

\section{Módulo de Recuperação}

O módulo de recuperação utiliza busca por similaridade cossenoidal sobre o índice
HNSW do ChromaDB. Dado um query do usuário, os $k$ trechos mais similares
são recuperados e concatenados como contexto para o LLM.

Após calibração experimental por meio de execuções RAGAS, o parâmetro $k$ foi
fixado em \textbf{5}. Valores menores ($k=3, 4$) excluíam, com frequência,
trechos relevantes em consultas envolvendo populações especiais ou interações
medicamentosas; valores maiores ($k \geq 7$) introduziam ruído sem ganho de
\textit{recall}. Varredura quantitativa $k \in \{3, 5, 7, 10\}$ sobre o
pipeline final consta no Apêndice~\ref{ape:topk-sweep}: $k=5$ produz o melhor
equilíbrio em 3 das 4 métricas, com diferença em \textit{Faithfulness} para o
melhor valor ($k=10$) dentro do envelope de variância inter-seed.

Adicionalmente, foi adotado um \textbf{limiar de similaridade cossenoidal de
$0{,}40$}. Consultas cujo \textit{top-1} apresenta score abaixo desse
limiar acionam um fallback que orienta o modelo a responder
explicitamente que a informação não foi encontrada no corpus, em lugar de
sintetizar uma resposta apoiada em fragmentos pouco relevantes. Essa salvaguarda
mitiga o risco de alucinação extrínseca em consultas fora do escopo dos
protocolos indexados.

O limiar foi definido empiricamente a partir da distribuição de scores
nas 38 questões de avaliação: valores acima de $0{,}40$ correlacionaram-se com
\textit{ground truths} extraíveis dos chunks recuperados, enquanto
valores abaixo coincidiam, em sua maioria, com tópicos fora do corpus.

\section{Módulo de Geração}

O modelo Llama~3.3~70B \cite{dubey2024} é acessado via API Groq. O \textit{prompt
system} instrui o modelo a responder exclusivamente com base no contexto recuperado,
em português do Brasil, e a indicar quando a informação não está disponível no
corpus.

O \textit{system prompt} de produção (versão~v1) instrui o modelo a: (i)
responder \textbf{exclusivamente} com base no contexto recuperado; (ii) usar
português brasileiro com terminologia clínica adequada à enfermagem; (iii)
declarar explicitamente quando a informação não estiver no contexto,
acionando o \textit{fallback} de similaridade descrito na seção anterior;
(iv) preservar números, doses e unidades exatamente como no protocolo
original; e (v) anexar referência à fonte (documento de origem) quando
disponível nos metadados. O texto integral do prompt está reproduzido
no Apêndice~\ref{ape:prompt}.

\section{Interface de Demonstração}

A interface de demonstração é uma aplicação web responsiva (HTML/CSS/JavaScript)
servida pelo FastAPI e acessível via túnel ngrok. A interface inclui aviso explícito
de que a ferramenta é de apoio e não substitui o julgamento clínico profissional.

\aluno{Inserir screenshot da interface de chat.}

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Avaliação e Resultados}
\label{cap:avaliacao}
% ─────────────────────────────────────────────────────────────────────────────

\section{Dataset de Avaliação}

O \textit{dataset} de avaliação RAGAS foi construído pelo autor a partir do corpus
indexado, totalizando \textbf{42 questões clínicas} sobre o manejo da ILTB, das
quais \textbf{38} são \textit{in-scope} (utilizadas no cálculo das métricas) e
\textbf{4} são \textit{out-of-scope}, destinadas exclusivamente ao teste do
fallback de similaridade descrito na Seção~\ref{cap:desenvolvimento}.

As 38 questões \textit{in-scope} estão organizadas em sete categorias temáticas
(Tabela~\ref{tab:dataset-cat}), que cobrem os principais eixos de consulta
esperados em uso real. A categorização serve a dois propósitos:
\textbf{(i)} garantir cobertura balanceada do escopo clínico durante a
construção do \textit{dataset} e \textbf{(ii)} permitir leitura descritiva
da decomposição das métricas RAGAS por subdomínio --- breakdown reportado
no Apêndice~\ref{ape:per-category}, com a ressalva metodológica de que
$N=4$ a $7$ questões por categoria não autoriza inferências estatísticas
comparativas entre elas. Os achados qualitativos desse breakdown (notadamente
a concentração de \textit{Faithfulness} baixa em categorias com alta
densidade numérica) são integrados à interpretação do paradoxo
abstrativo--vs--conservador na Seção~\ref{sec:interp-faith}.

\begin{table}[h]
  \centering
  \caption{Distribuição do \textit{dataset} de avaliação por categoria}
  \label{tab:dataset-cat}
  \begin{tabular}{@{}clc@{}}
  \toprule
  \textbf{Sigla} & \textbf{Categoria} & \textbf{N} \\
  \midrule
  ET   & Esquemas terapêuticos (6H, 9H, 4R, 3HP)            & 7 \\
  IND  & Indicações de tratamento                            & 5 \\
  PE   & Populações especiais (PVHIV, gestantes, crianças)   & 7 \\
  DI   & Diagnóstico (PT, IGRA, exclusão de TB ativa)        & 5 \\
  MO   & Monitoramento clínico e laboratorial                & 5 \\
  IT   & Interações medicamentosas                           & 5 \\
  EA   & Efeitos adversos                                    & 4 \\
  \midrule
  \multicolumn{2}{l}{\textit{Total \textit{in-scope}}}        & \textbf{38} \\
  FE   & Fora do escopo (apenas para teste do fallback) & 4 \\
  \midrule
  \multicolumn{2}{l}{\textit{Total geral}}                     & \textbf{42} \\
  \bottomrule
  \end{tabular}
  \caption*{Fonte: Produção do próprio autor. Dataset completo no
            Apêndice~\ref{ape:dataset}.}
\end{table}

Os \textit{ground truths} foram extraídos \textbf{literalmente} dos arquivos
Markdown indexados, preservando trechos verbatim do MS ou da OMS. Essa decisão
metodológica fortalece a interpretação das métricas RAGAS: como o \textit{ground
truth} é, por construção, recuperável do corpus, eventuais falhas no
\textit{Context Recall} indicam problemas reais de recuperação --- não ausência
de informação na base.

Quatro questões originalmente incluídas tratavam de tópicos \textbf{fora do
escopo} (\textit{out-of-scope}), destinadas a testar o fallback de
similaridade descrito na Seção~\ref{cap:desenvolvimento}; elas são excluídas dos
cálculos RAGAS, totalizando $36$--$38$ amostras válidas por execução conforme
disponibilidade do LLM-juiz.

O LLM-juiz adotado para a avaliação foi o \texttt{gpt-4o-mini} (OpenAI),
escolhido por três motivos: (i) suporte a $n>1$ \textit{completions}, necessário
para o cálculo correto de \textit{Faithfulness}; (ii) ausência de limite TPM
restritivo que inviabilize execuções completas; (iii) custo aproximado de
US\$\,$0{,}05$ por execução de 38 questões. Importante notar que o
\textit{gpt-4o-mini} foi usado \textbf{exclusivamente como avaliador},
permanecendo o Llama~3.3~70B via Groq como o gerador no pipeline de
produção.

\section{Avaliação Automatizada (RAGAS)}

A Tabela~\ref{tab:ragas} apresenta a evolução dos resultados ao longo das principais
configurações testadas.

\begin{table}[ht]
  \caption{Evolução dos resultados RAGAS do chatbot ILTB (marcos de
           desenvolvimento; uma corrida por linha, sem replicação multi-seed
           --- ver Limitação L7)}
  \label{tab:ragas}
  \begin{tabularx}{\textwidth}{@{}Xcccc@{}}
  \toprule
  \textbf{Configuração} &
  \textbf{Faith.} &
  \textbf{Ans. Rel.} &
  \textbf{Ctx. Prec.} &
  \textbf{Ctx. Rec.} \\
  \midrule
  Baseline (pré-sanitização, top\_k=4) & 0{,}375 & 0{,}310 & 0{,}548 & 0{,}382 \\
  Pós-sanitização, prompt v1, top\_k=4 & 0{,}586 & 0{,}534 & 0{,}656 & 0{,}608 \\
  A/B gpt-4o-mini como pipeline LLM     & 0{,}525 & 0{,}524 & 0{,}657 & 0{,}583 \\
  \textbf{Chunker hierárquico, top\_k=5 (final)} &
    \textbf{0{,}515} & \textbf{0{,}381} & \textbf{0{,}735} & \textbf{0{,}520} \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor.}
\end{table}

A Tabela~\ref{tab:ragas} sintetiza quatro pontos da trajetória de otimização, dos
quais decorrem as seguintes leituras:

\textbf{Linha 1 (baseline).} Configuração inicial sem sanitização de
chunks e com $k=4$. Os scores baixos em todas as métricas
indicam principalmente falhas de recuperação: \textit{Context Precision} de
$0{,}548$ confirma que aproximadamente metade dos chunks recuperados
não eram relevantes para a pergunta, propagando essa falha para
\textit{Faithfulness} e \textit{Answer Relevancy}.

\textbf{Linha 2 (pós-sanitização, prompt v1).} A introdução de
sanitização Markdown (Seção~\ref{cap:desenvolvimento}) e a primeira versão
estável do \textit{system prompt} elevaram \textit{Faithfulness} de
$0{,}375$ para $0{,}586$ ($+56\%$) e duplicaram o \textit{Answer Relevancy}
($0{,}310 \to 0{,}534$). Esse salto absorveu, em uma única iteração, as
duas correções de maior impacto da Fase 2: a remoção de artefatos de
extração do PDF e a estabilização do \textit{prompt} de produção.

\textbf{Linha 3 (\textit{gpt-4o-mini} no pipeline).} Teste A/B
substituindo o Llama~3.3~70B pelo \texttt{gpt-4o-mini} também como gerador
(não apenas como juiz). Os resultados \textit{não} mostraram superioridade
relevante, e a opção foi descartada, mantendo o Llama por razões de custo e
licença open-weight. Esta linha não substitui a ablação $2 \times 2$ da
Seção~\ref{sec:ablacao-2x2}, que testa o GPT-4o (modelo maior, não o
\texttt{gpt-4o-mini}) em design fatorial cruzado com a dimensão
\emph{embedding} (MiniLM 384D vs BGE-M3 1024D) --- comparação que esta
linha 3 não isolava.

\textbf{Linha 4 (encerramento da Fase 2).} Adoção do \textit{chunker} hierárquico
descrito na Seção~\ref{cap:desenvolvimento} e $k=5$. O \textit{Context Precision}
alcançou $0{,}735$ --- próximo à meta de $0{,}75$ ---, indicando que a recuperação
estabilizou em torno de um patamar útil. O \textit{Faithfulness}, em contrapartida,
permaneceu em $0{,}515$, abrindo a questão diagnóstica investigada pela ablação
$2 \times 2$ apresentada na Seção~\ref{sec:ablacao-2x2}.

\textbf{Nota sobre comparabilidade com a Tabela~\ref{tab:ablacao-2x2}.} A linha
4 desta tabela registra a corrida única histórica do encerramento da Fase 2
(\textit{Faithfulness} $0{,}515$; \textit{Context Precision} $0{,}735$). A
Tabela~\ref{tab:ablacao-2x2} reporta, na sua primeira célula, a mesma
configuração técnica (Llama~+~MiniLM~+~denso) sob replicação multi-seed
posterior, com média $0{,}486 \pm 0{,}030$ / $0{,}736 \pm 0{,}007$ (3 seeds).
A diferença em \textit{Faithfulness} ($-0{,}029$) está dentro de 1~DP da
medição replicada e é compatível com a variância inter-seed do juiz LLM que
motivou a replicação. \textbf{Para toda análise comparativa e inferência de
efeitos (Capítulo~\ref{cap:avaliacao} a partir da
Seção~\ref{sec:ablacao-2x2}), adota-se a Tabela~\ref{tab:ablacao-2x2} como
\emph{baseline} canônico do trabalho}; a Tabela~\ref{tab:ragas} permanece
como registro cronológico do desenvolvimento.

\subsection{Ablação 2×2: Gerador e Embedding}
\label{sec:ablacao-2x2}

O patamar de \textit{Faithfulness} observado ao fim da Fase 2 admitia duas
explicações concorrentes: (i) limitação do \textbf{gerador} --- o Llama~3.3~70B
seria intrinsecamente insuficiente para sintetizar respostas fiéis ao contexto
no domínio ILTB; ou (ii) limitação do pipeline \textbf{RAG} ---
retriever/embedding insuficientes para entregar contexto rico,
forçando o gerador a respostas pobres. A literatura de RAG clínico recomenda,
exatamente nessa situação, uma ablação cruzada isolando cada variável
\cite{ocaf2024, wang2024bestpractices}.

Realizou-se, portanto, uma ablação $2 \times 2$ sobre o mesmo conjunto de 38
questões \textit{in-scope} (Seção anterior), variando dois fatores enquanto
mantendo o restante do pipeline inalterado: o \textbf{gerador}
(Llama~3.3~70B vs GPT-4o) e o \textbf{embedding}
(\texttt{paraphrase-multilingual-MiniLM-L12-v2} 384D vs \texttt{BAAI/bge-m3}
1024D). Cada uma das quatro células foi executada em \textbf{três seeds
independentes} para permitir o cálculo de média e desvio padrão das métricas
RAGAS, conforme recomendado pela literatura para experimentos com juiz LLM
estocástico \cite{es2024, ocaf2024}; o juiz RAGAS foi mantido constante em
\texttt{gpt-4o-mini} (temperatura 0). A Tabela~\ref{tab:ablacao-2x2}
apresenta os resultados na forma \textit{média}~$\pm$~\textit{desvio padrão}
sobre as três seeds.

\begin{table}[ht]
  \caption{Ablação $2 \times 2$: Gerador × Embedding (38 questões; 3 seeds
           por célula; média~$\pm$~DP; juiz RAGAS \texttt{gpt-4o-mini})}
  \label{tab:ablacao-2x2}
  \begin{tabularx}{\textwidth}{@{}lXcccc@{}}
  \toprule
  \textbf{Gerador} & \textbf{Embedding} &
  \textbf{Faith.} & \textbf{Ans. Rel.} & \textbf{Ctx. Prec.} & \textbf{Ctx. Rec.} \\
  \midrule
  Llama~3.3~70B & MiniLM 384D & $0{,}486 \pm 0{,}030$ & $0{,}328 \pm 0{,}030$ & $0{,}736 \pm 0{,}007$ & $0{,}515 \pm 0{,}073$ \\
  GPT-4o        & MiniLM 384D & $0{,}395 \pm 0{,}004$ & $0{,}297 \pm 0{,}014$ & $0{,}751 \pm 0{,}015$ & $0{,}555 \pm 0{,}023$ \\
  GPT-4o        & BGE-M3 1024D & $0{,}620 \pm 0{,}011$ & $0{,}625 \pm 0{,}011$ & $0{,}936 \pm 0{,}011$ & $0{,}776 \pm 0{,}014$ \\
  \textbf{Llama~3.3~70B} & \textbf{BGE-M3 1024D}
        & $\mathbf{0{,}679 \pm 0{,}004}$ & $\mathbf{0{,}660 \pm 0{,}019}$ & $\mathbf{0{,}923 \pm 0{,}003}$ & $\mathbf{0{,}751 \pm 0{,}020}$ \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor.}
\end{table}

\textbf{Efeito do embedding (linhas pareadas, gerador fixo).}
Mantendo o gerador GPT-4o constante, a substituição MiniLM 384D → BGE-M3 1024D
produziu ganhos de $+56{,}8\%$ em \textit{Faithfulness}
($0{,}395 \to 0{,}620$), $+110{,}2\%$ em \textit{Answer Relevancy}
($0{,}297 \to 0{,}625$), $+24{,}7\%$ em \textit{Context Precision}
($0{,}751 \to 0{,}936$, ultrapassando confortavelmente a meta de $0{,}75$)
e $+39{,}8\%$ em \textit{Context Recall} ($0{,}555 \to 0{,}776$). Mantendo
o gerador Llama constante, a mesma substituição produziu ganhos de
$+39{,}7\%$, $+101{,}2\%$, $+25{,}4\%$ e $+45{,}9\%$, respectivamente. Em
ambos os geradores, portanto, a migração de embedding produz ganho
expressivo em todas as métricas, com magnitudes várias vezes superiores
ao envelope de incerteza inter-seed reportado na
Tabela~\ref{tab:ablacao-2x2} (e.g., $\Delta \approx 0{,}19$ em
\textit{Faithfulness} para o gerador Llama contra DP máximo de $\pm 0{,}030$,
razão $\approx 6{,}5$); a interpretação aqui é de \emph{tamanho de efeito
relativo à variância observada}, não de teste de hipótese formal --- que
exigiria $N$ maior do que 3 seeds. A direção e magnitude observadas são
consistentes com os \textit{benchmarks} multilíngues
\cite{SHTI2023, mmteb2025} e com a revisão sistemática de
\citeauthor{ocaf2024} \cite{ocaf2024}, que identifica a escolha de embedding
como o principal lever de qualidade em \textit{pipelines} RAG clínicos.

\textbf{Efeito do gerador (colunas pareadas, embedding fixo).}
Surpreendentemente, substituir o Llama pelo GPT-4o \emph{piora} a
\textit{Faithfulness} em ambos os embeddings: $0{,}486 \to 0{,}395$ no
MiniLM ($-18{,}7\%$, com DPs $\pm 0{,}030$ e $\pm 0{,}004$) e
$0{,}679 \to 0{,}620$ no BGE-M3 ($-8{,}7\%$, com DPs $\pm 0{,}004$ e
$\pm 0{,}011$). Em ambas as comparações pareadas, a magnitude do efeito
($\Delta = 0{,}091$ e $0{,}059$, respectivamente) é várias vezes superior
ao DP inter-seed máximo de cada par, padrão que indica efeito direcional
robusto ainda que sem teste de hipótese formal. O \textit{Context Precision} permanece
estatisticamente equivalente entre os dois geradores (esperado --- essa
métrica depende do retriever, não do gerador). \textit{Answer Relevancy}
também cai com GPT-4o em ambos os embeddings. A inspeção direta das respostas geradas revelou a causa:
o GPT-4o segue mais estritamente as instruções do \textit{system prompt}
(Apêndice~\ref{ape:prompt}) e produz, com maior frequência, a resposta de
fallback literal --- ``Não encontrei essa informação nos protocolos
indexados.'' --- mesmo em casos em que o conteúdo está presente nos
chunks recuperados. O Llama, em contraste, sintetiza a partir dos
chunks mesmo quando o protocolo de origem é parcial, comportamento que
o RAGAS pontua como mais fiel porque há claims verificáveis na resposta.

Esse efeito --- modelo mais alinhado a instruções pontuando \emph{pior} em
métricas automatizadas --- é convergente com observações independentes em RAG
clínico: \citeauthor{zakka2024almanac} \cite{zakka2024almanac}, no sistema
Almanac (NEJM AI), relata que modelos de fronteira frequentemente recusam
responder em contextos clínicos e exigem engenharia específica para liberar a
síntese quando segura. A literatura de mitigação de alucinação em LLMs
\cite{huang2023halucination, ocaf2024} descreve esse padrão como ``abstenção
excessiva'' --- evitar alucinação ao custo de cobertura. O paper original do
RAGAS \cite{es2024} reconhece, igualmente, que sua métrica de
\textit{Faithfulness} pode penalizar respostas seguras-por-recusa em cenários
de síntese.

\textbf{Limitação do juiz adotado.} O paradoxo descrito acima é robusto sob
replicação multi-seed (Tabela~\ref{tab:ablacao-2x2}), mas o juiz RAGAS
utilizado --- \texttt{gpt-4o-mini} --- pertence à mesma família comercial
(OpenAI) que um dos geradores candidatos da ablação (GPT-4o). Há, portanto,
risco residual de \textbf{viés de avaliador intra-família}: o juiz pode
sistematicamente penalizar ou favorecer respostas produzidas por modelos da
mesma família por afinidade estilística (e.g., tendência a tratar o
\emph{fallback} literal do GPT-4o como falta de \emph{claims} verificáveis,
quando um juiz de outra família poderia ler como abstenção apropriada). Esse
risco não foi controlado neste trabalho: a comparação Llama \emph{vs.}
GPT-4o no juízo de \texttt{gpt-4o-mini} não foi replicada com avaliador
independente (e.g., Claude, Gemini, ou \textit{ensemble} de juízes). A
direção do efeito (Llama $>$ GPT-4o em \textit{Faithfulness}) é convergente
com a literatura de RAG clínico citada acima, o que reduz --- mas não
elimina --- a possibilidade de o paradoxo ser parcialmente artefato do
juiz. Replicação com juiz de família independente está registrada em
Trabalhos Futuros como TF de avaliação metodológica.

\subsection{Configuração Pós-Migração (Intermediária)}
\label{sec:gate-pos-migracao}

A célula \textbf{Llama~3.3~70B + BGE-M3 1024D} (Tabela~\ref{tab:ablacao-2x2},
linha 4) constituiu o gate intermediário do artefato após a migração de
embedding. A comparação pareada com a célula Llama~+~MiniLM da mesma ablação
mostra ganhos uniformes em todas as métricas: \textit{Faithfulness}
$0{,}486 \pm 0{,}030 \to 0{,}679 \pm 0{,}004$ ($+39{,}7\%$);
\textit{Answer Relevancy} $0{,}328 \pm 0{,}030 \to 0{,}660 \pm 0{,}019$
($+101{,}2\%$); \textit{Context Precision}
$0{,}736 \pm 0{,}007 \to 0{,}923 \pm 0{,}003$ ($+25{,}4\%$, agora PASS robusto
sobre a meta $\geq 0{,}75$); \textit{Context Recall}
$0{,}515 \pm 0{,}073 \to 0{,}751 \pm 0{,}020$ ($+45{,}9\%$).

A \textit{Faithfulness} de $0{,}679$ permanece abaixo da meta original de
$0{,}80$, mas o \textbf{diagnóstico do gap} se alterou substancialmente. Com
\textit{Context Precision} em $0{,}923$, o problema deixou de ser de
\textbf{recuperação} e tornou-se de \textbf{geração}: o retriever
entrega chunks relevantes, mas o gerador ainda produz, em parte das
questões, afirmações que o juiz RAGAS classifica como não estritamente
derivadas do contexto. Esse diagnóstico motivou três intervenções
pós-recuperação subsequentes, descritas nas Seções~\ref{sec:hybrid}
(busca híbrida com BM25), \ref{sec:rerank} (reranking
cross-encoder) e \ref{sec:chunk-test} (tuning de \textit{chunk\_size}),
fechando a iteração experimental do trabalho.

\subsection{Iteração 1: Busca Híbrida com BM25 e RRF}
\label{sec:hybrid}

A primeira intervenção pós-recuperação adotou busca híbrida combinando o
retriever denso BGE-M3 já em produção com um índice esparso BM25
(\citeauthor{cormack2009}, \citeyear{cormack2009}; \citeauthor{formal2021},
\citeyear{formal2021}), fundindo os dois rankings via
\textit{Reciprocal Rank Fusion} (RRF). A hipótese é que termos clínicos exatos
(siglas como ``PVHIV'' e ``3HP'', dosagens, nomes de fármacos) e construções
com negação respondem melhor a casamento lexical, complementando o
retriever densamente codificado.

O índice BM25 é construído como \textit{sidecar} do ChromaDB --- compartilha
os mesmos 898 chunks indexados, sem duplicação. Cada consulta gera
20 candidatos de cada \textit{ranker}, e o RRF (constante $k = 60$, conforme
\citeauthor{cormack2009}) computa o score final
$\sum_{r} 1 / (k + \text{rank}_r(d))$, do qual se selecionam os $k = 5$
chunks de maior rank fundido.

A Tabela~\ref{tab:hybrid-rerank} (linha 2) apresenta o resultado da avaliação
RAGAS sobre as mesmas 38 questões, agregada sobre 3 seeds independentes. O
ganho mais expressivo foi em \textbf{\textit{Faithfulness}}
($0{,}679 \pm 0{,}004 \to 0{,}740 \pm 0{,}017$, $+9{,}0\%$; $\Delta = 0{,}061$,
magnitude $\approx 3{,}6\times$ o DP máximo do par), seguido por
\textit{Answer Relevancy}
($0{,}660 \pm 0{,}019 \to 0{,}707 \pm 0{,}036$, $+7{,}1\%$) e
\textit{Context Recall} ($0{,}751 \pm 0{,}020 \to 0{,}815 \pm 0{,}025$,
$+8{,}5\%$): o BM25 captura chunks com termos exatos (siglas como ``3HP'',
``PVHIV'', dosagens) que o BGE-M3 não recuperava, e o gerador, com cobertura
mais completa, sintetiza respostas mais ancoradas no contexto. O ganho em
\textit{Faithfulness} recai exatamente no envelope previsto pela literatura
($+5$ a $+10$\,pp, \citeauthor{wang2024bestpractices},
\citeyear{wang2024bestpractices}). Houve regressão marginal em
\textit{Context Precision} ($0{,}923 \pm 0{,}003 \to 0{,}915 \pm 0{,}008$,
$-0{,}9\%$, dentro de 1~DP), \textit{trade-off} esperado da busca lexical:
candidatos BM25 ocasionalmente promovem chunks lexicalmente similares mas
semanticamente periféricos. Um efeito secundário relevante foi a melhoria do
comportamento de fallback em consultas \textit{out-of-scope}: scores RRF
nessas consultas caíram para $\sim 0{,}03$ (vs. $\sim 0{,}87$ no
\textit{retrieval} denso puro), facilitando rejeição segura por limiar
simples.

\subsection{Iteração 2: Reranking Cross-encoder (mGTE)}
\label{sec:rerank}

Sobre o pipeline híbrido da Seção anterior, adicionou-se uma etapa
de reranking com o cross-encoder multilíngue
\texttt{Alibaba-NLP/gte-multilingual-reranker-base} (\citeauthor{zhang2024mgte},
\citeyear{zhang2024mgte}). \textit{Cross-encoders} processam o par
(query, chunk) conjuntamente, capturando relevância
fine-grained que \textit{bi-encoders} densos como o BGE-M3 e modelos esparsos
como o BM25 não capturam isoladamente.

O fluxo final é: busca densa top-20 $\cup$ BM25 top-20 $\to$ fusão RRF $\to$
cross-encoder reordena $\to$ top-5 enviado ao gerador. O custo
computacional é aceitável (\textasciitilde{}200\,ms adicionais por consulta
em CPU), sem necessidade de GPU dedicada.

A Tabela~\ref{tab:hybrid-rerank} (linha 3) sumariza o resultado, agregado sobre
3 seeds. A contribuição dominante do reranker foi em
\textbf{\textit{Context Precision}}, restaurando-a ao patamar de $0{,}956
\pm 0{,}009$ ($+4{,}5\%$ sobre o híbrido sem rerank, recuperando o regime
denso e ultrapassando-o em PASS robusto). \textit{Faithfulness} apresentou
ganho mais modesto ($0{,}740 \pm 0{,}017 \to 0{,}752 \pm 0{,}004$, $+1{,}6\%$,
dentro do envelope de incerteza inter-seed): o ganho está direcionalmente
correto e --- importante --- a configuração com reranker apresenta DP de
$\pm 0{,}004$ contra $\pm 0{,}017$ do híbrido sem rerank, indicando que o
reranker \textbf{estabiliza} a métrica entre seeds, não apenas melhora o
patamar médio. \textit{Answer Relevancy} subiu marginalmente ($+2{,}5\%$,
dentro de 1~DP) e \textit{Context Recall} ficou estatisticamente equivalente
($0{,}815 \pm 0{,}025 \to 0{,}814 \pm 0{,}003$). Em síntese:
\textbf{o ganho dominante em \textit{Faithfulness} veio do híbrido BM25+denso
(Seção~\ref{sec:hybrid})}; o reranker mGTE, por sua vez, é a peça que
consolida \textit{Context Precision} acima de $0{,}95$ e reduz variância
entre seeds. Ambos justificam a inclusão no gate final, mas por razões
métricas distintas.

\begin{table}[ht]
  \caption{Iterações pós-recuperação aplicadas sobre Llama~3.3~70B + BGE-M3
           1024D (3 seeds por linha; média~$\pm$~DP)}
  \label{tab:hybrid-rerank}
  \begin{tabularx}{\textwidth}{@{}Xcccc@{}}
  \toprule
  \textbf{Configuração} & \textbf{Faith.} & \textbf{Ans. Rel.} &
  \textbf{Ctx. Prec.} & \textbf{Ctx. Rec.} \\
  \midrule
  Pós-migração (denso BGE-M3)         & $0{,}679 \pm 0{,}004$ & $0{,}660 \pm 0{,}019$ & $0{,}923 \pm 0{,}003$ & $0{,}751 \pm 0{,}020$ \\
  + Busca híbrida (BGE-M3 + BM25, RRF) & $0{,}740 \pm 0{,}017$ & $0{,}707 \pm 0{,}036$ & $0{,}915 \pm 0{,}008$ & $0{,}815 \pm 0{,}025$ \\
  \textbf{+ Reranker mGTE (gate final)}
        & $\mathbf{0{,}752 \pm 0{,}004}$ & $\mathbf{0{,}725 \pm 0{,}020}$ & $\mathbf{0{,}956 \pm 0{,}009}$ & $\mathbf{0{,}814 \pm 0{,}003}$ \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor. 38 questões \textit{in-scope};
            3 seeds independentes por linha; juiz RAGAS \texttt{gpt-4o-mini}
            (temperatura 0).}
\end{table}

\subsection{Iteração 3: Teste de chunk\_size}
\label{sec:chunk-test}

A literatura \cite{wang2024bestpractices} sugere que chunks menores
(300--500 tokens) otimizam o trade-off entre fidelidade e cobertura
em domínios técnicos. Avaliou-se, portanto, a re-ingestão do corpus com
\texttt{CHUNK\_SIZE = 500} caracteres (vs. 800 da configuração padrão), sobre
o pipeline \textit{hybrid+rerank} já estabilizado. Resultados sobre
as mesmas 38 questões (corrida única, sem replicação seed): \textit{Faithfulness}
$0{,}756$, \textit{Answer Relevancy} $0{,}683$, \textit{Context Precision}
$0{,}951$, \textit{Context Recall} $0{,}811$. Comparando com o gate final
da Tabela~\ref{tab:hybrid-rerank} (3 seeds), as diferenças observadas em
Faith. e Ctx. Prec. ficam dentro de 1~DP do gate, configurando \textit{efeito
nulo}; \textit{Answer Relevancy} apresenta queda mais pronunciada ($-5{,}8\%$),
sugerindo regressão real, ainda que o teste não tenha sido replicado.

Diagnóstico do efeito nulo: o \textit{chunker} hierárquico do
pipeline (descrito na Seção~\ref{cap:desenvolvimento}) é guiado
pelas fronteiras de cabeçalho do \textit{Markdown} sanitizado e por um piso
\texttt{MIN\_CHUNK\_SIZE = 400} \textit{chars}. Reduzir o teto de 800 para
500 \textit{chars} alterou apenas marginalmente a distribuição de tamanhos
(mediana de \textasciitilde{}800 para \textasciitilde{}674 \textit{chars}),
preservando a maior parte da granularidade original. Para um teste estrito
do efeito descrito por \citeauthor{wang2024bestpractices} seria necessário
substituir o \textit{chunker} por uma estratégia baseada em tokens
do modelo (\textit{e.g.}, Small-to-Big \cite{advancedChunkingRAG2025}),
mudança fora do escopo desta iteração. O \texttt{CHUNK\_SIZE = 800} foi,
portanto, mantido.

\subsection{Configuração Final do Artefato}
\label{sec:gate-final}

A combinação Llama~3.3~70B + BGE-M3 1024D + busca híbrida (BM25 + denso, RRF
$k = 60$) + reranker mGTE foi adotada como \textbf{gate operacional
final do artefato}. A Tabela~\ref{tab:gate-final} consolida os
scores comparados à célula Llama~+~MiniLM da ablação --- adotada
como \textit{baseline} canônico (Seção~\ref{sec:ablacao-2x2}, primeira
célula). Esta escolha substitui o \textit{baseline} de corrida única
historicamente registrado na Tabela~\ref{tab:ragas} (linha 4,
\textit{Faithfulness} $0{,}515$) por uma medição replicada da mesma
configuração técnica em 3 seeds independentes ($0{,}486 \pm 0{,}030$):
ambas medem o mesmo \textit{pipeline} (Llama~+~MiniLM~+~denso), mas a
forma replicada é a comparação metodologicamente apropriada para
quantificar o ganho do gate final. \textbf{Os deltas relativos desta
tabela e os da Tabela~\ref{tab:ragas} \emph{não são intercambiáveis}}:
deslocar o \textit{baseline} de $0{,}515$ (corrida única) para
$0{,}486 \pm 0{,}030$ (3 seeds) eleva o ganho relativo de
\textit{Faithfulness} de $+47{,}8\%$ para $+54{,}7\%$; ambos refletem a
mesma magnitude absoluta ($\Delta \approx +0{,}24$ a $+0{,}27$) sob
referenciais diferentes.

\begin{table}[ht]
  \caption{Gate operacional final vs. célula Llama+MiniLM da ablação
           (\textit{baseline} canônico, 3 seeds da Tabela~\ref{tab:ablacao-2x2}).
           Gate final: 3 seeds, média~$\pm$~DP. Deltas calculados sobre médias.}
  \label{tab:gate-final}
  \begin{tabularx}{\textwidth}{@{}Xcccc@{}}
  \toprule
  \textbf{Configuração} &
  \textbf{Faith.} & \textbf{Ans. Rel.} &
  \textbf{Ctx. Prec.} & \textbf{Ctx. Rec.} \\
  \midrule
  Llama + MiniLM 384D + denso (baseline ablação)
        & $0{,}486 \pm 0{,}030$ & $0{,}328 \pm 0{,}030$ & $0{,}736 \pm 0{,}007$ & $0{,}515 \pm 0{,}073$ \\
  \textbf{Gate Final} (BGE-M3 + híbrido + rerank)
        & $\mathbf{0{,}752 \pm 0{,}004}$ & $\mathbf{0{,}725 \pm 0{,}020}$ & $\mathbf{0{,}956 \pm 0{,}009}$ & $\mathbf{0{,}814 \pm 0{,}003}$ \\
  \midrule
  \textit{$\Delta$ absoluto (médias)} &
    $+0{,}266$ & $+0{,}397$ & $+0{,}220$ & $+0{,}299$ \\
  \textit{$\Delta$ relativo} &
    $+54{,}7\%$ & $+121{,}0\%$ & $+29{,}9\%$ & $+58{,}1\%$ \\
  \textit{Gate (faith $\geq 0{,}80$; ctx\_prec $\geq 0{,}75$)} &
    FAIL / FAIL & — & \textbf{FAIL / PASS} & — \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor.}
\end{table}

\textit{Context Precision} de $0{,}956 \pm 0{,}009$ ultrapassa
confortavelmente a meta de $0{,}75$ (margem absoluta de $+0{,}206$ sobre o
alvo). O intervalo observado entre as 3 seeds, $[0{,}949;\, 0{,}966]$,
permanece integralmente acima da meta, sustentando a leitura de
\textbf{PASS robusto} como indicador-síntese do gate final. \textit{Faithfulness} de $0{,}752 \pm 0{,}004$ está a $\sim 0{,}048$
da meta original de $0{,}80$ --- gap residual interpretado na
Seção~\ref{sec:interp-faith} e endereçado, como linha de pesquisa imediata,
no Capítulo~\ref{cap:conclusao}.

\subsection{Interpretação da Métrica de Faithfulness}
\label{sec:interp-faith}

O patamar de \textit{Faithfulness} no gate final do artefato,
$0{,}752 \pm 0{,}004$ (Tabela~\ref{tab:gate-final}), está acima do que a
literatura de RAG clínico reporta como teto comum sem intervenções de
self-reflection: a revisão sistemática de \citeauthor{ocaf2024}
\cite{ocaf2024}, combinando 30 estudos do domínio, indica que valores acima
de $0{,}75$ raramente são atingidos sem mecanismos adicionais sobre o esquema
\textit{retrieve-then-generate} básico. As três iterações pós-recuperação
descritas nas Seções \ref{sec:hybrid}--\ref{sec:gate-final} (busca híbrida,
reranking cross-encoder e teste de \textit{chunk\_size}) fecharam
aproximadamente $85\%$ do gap original que separava a célula Llama+MiniLM
da ablação ($0{,}486$) da meta de $0{,}80$.

O gap residual de $\sim 0{,}048$ tem três fatores complementares descritos na
literatura, ainda presentes no pipeline mesmo após as intervenções. O
breakdown por categoria do gate final (Apêndice~\ref{ape:per-category})
fornece evidência empírica direta de que o gap se concentra nas categorias
com alta densidade numérica do \textit{dataset} (esquemas terapêuticos,
populações especiais, interações medicamentosas) --- exatamente onde o
gerador é forçado a sintetizar dose, duração e regimes em vez de reproduzir
o texto literal do protocolo, perdendo correspondência verbatim que o RAGAS
exige.

\textbf{Comportamento abstrativo do gerador.} Modelos generativos como o
Llama~3.3~70B foram caracterizados desde \citeauthor{lewis2021}
\cite{lewis2021} como abstrativos: o decodificador sintetiza informação dos
chunks em vez de reproduzi-los literalmente. Essa síntese é, conforme
o próprio paper seminal aponta, \textit{vantagem} para tarefas de
pergunta-resposta abertas, mas a métrica \textit{Faithfulness} do RAGAS
\cite{es2024} mede correspondência literal entre afirmações e contexto ---
penalizando o que pode ser paráfrase clinicamente correta. A ablação $2 \times 2$
(Seção~\ref{sec:ablacao-2x2}) já evidenciara esse efeito: substituir o Llama
por GPT-4o, com seu comportamento mais prompt-adherent, piorou
\textit{Faithfulness} em vez de melhorá-la.

\textbf{Granularidade do chunk.} \citeauthor{gao2024} \cite{gao2024}
identifica o tamanho do chunk como um dos principais determinantes
do \textit{trade-off} entre fidelidade literal e qualidade da resposta.
\citeauthor{wang2024bestpractices} \cite{wang2024bestpractices} sustenta
empiricamente que chunks de 300--500 tokens, combinados com
reranking, otimizam esse trade-off em domínios técnicos. O teste
descrito na Seção~\ref{sec:chunk-test} reduziu o \texttt{CHUNK\_SIZE} de 800
para 500 \textit{chars} e não produziu ganho mensurável, porque o
\textit{chunker} hierárquico do pipeline é dominado por fronteiras
de cabeçalho do \textit{Markdown} e pelo piso \texttt{MIN\_CHUNK\_SIZE = 400}.
Para ativar o efeito previsto pela literatura seria necessária a substituição
do \textit{chunker} por uma estratégia baseada em tokens do modelo
(\textit{e.g.}, Small-to-Big \cite{advancedChunkingRAG2025}), tarefa
registrada como trabalho futuro.

\textbf{Negação e nuances clínicas.} O domínio ILTB tem alta densidade de
regras condicionais (``tratar se X, exceto se Y'') e termos com semântica
oposta próxima (``elegível / contraindicado''), que \textit{retrievers}
densos atuais não capturam tão bem quanto modelos esparsos
\cite{cormack2009, formal2021}. A introdução de BM25 e do reranker
mGTE (Seções~\ref{sec:hybrid} e \ref{sec:rerank}) mitigou parcialmente esse
limite, mas \textit{bi-encoders} dense permanecem suscetíveis a confundir
afirmações negadas e suas contrapartes afirmativas
\cite{huang2023halucination}.

A combinação desses três fatores residuais explica por que o pipeline
estabiliza em $0{,}752 \pm 0{,}004$ em vez de atingir $0{,}80$. A próxima intervenção
prevista pela literatura --- self-reflection sobre a resposta gerada
\cite{jeong2024selfbiorag} --- exige reformulação arquitetural com custo
desproporcional ao gap residual ($+8$ a $+15$\,pp esperados, ao preço de
dobrar as chamadas ao gerador), e está registrada como linha de pesquisa
prioritária no Capítulo~\ref{cap:conclusao}.

A revisão de \citeauthor{ocaf2024} \cite{ocaf2024} reforça ainda um ponto
metodológico relevante: a avaliação automatizada por RAGAS deve ser
\textbf{complementada} por avaliação humana estruturada, especialmente para
sistemas em que síntese clinicamente correta é desejável --- princípio
endossado também pela diretriz de relato \textit{TRIPOD-LLM}
\cite{gallifant2025tripod}. A meta-análise de 30 estudos clínicos com RAG
relatada por \citeauthor{ocaf2024} mostra divergência estatística entre as
duas formas de avaliação: o ganho de utilidade percebido por humanos
apresenta \textit{odds ratio} de $1{,}65$, enquanto métricas automáticas
captam apenas $1{,}20$ --- sugerindo que o RAGAS \emph{subestima
sistematicamente} a utilidade clínica real de sistemas RAG em saúde
\cite{ocaf2024}. Esse argumento motiva o desenho da avaliação com
especialista descrita na Seção~\ref{sec:sessao-expert}, que complementa as
métricas automatizadas com uma rubrica clínica especializada e adiciona, em
particular, sensibilidade ao \textit{trade-off} segurança-vs-cobertura que
o RAGAS por si só não captura.

\section{Avaliação com Especialista}
\label{sec:sessao-expert}

A avaliação automatizada RAGAS, por suas limitações intrínsecas
(Seção~\ref{cap:referencial}), foi complementada por uma sessão estruturada com
especialista em ILTB. O delineamento dessa etapa segue as recomendações da literatura
de RAG biomédico, que reporta número médio de quatro avaliadores em estudos do
domínio --- variando de um a dez --- e enfatiza que avaliações com $N$ reduzido devem
ser estruturadas em torno de instrumentos multidimensionais, transformando o
especialista em consultor de validação técnica em lugar de sujeito estatístico
\cite{ocaf2024}.

\textbf{Nota sobre o uso do TAM-AIN.} O \textit{Technology Acceptance Model for AI
in Nursing} \cite{nursingTAM2024} é um instrumento psicométrico cuja validade
original depende de análise fatorial confirmatória e estatísticas de
consistência interna (e.g., $\alpha$ de Cronbach), procedimentos cuja
aplicabilidade depende do tamanho da amostra de avaliadores efetivamente
obtida na sessão. Duas situações são contempladas neste protocolo:

\begin{itemize}
  \item \textbf{$N=1$ avaliador} (cenário-base do TCC): a coleta adota as
        quatro dimensões do TAM-AIN --- alinhamento ético, prontidão
        organizacional, preservação da identidade profissional e
        capacidade de infraestrutura --- como \emph{eixos de roteiro} de
        uma entrevista semi-estruturada qualitativa. Os relatos do
        especialista são analisados como evidência narrativa por dimensão,
        sem agregação em escores --- análise fatorial e estatísticas de
        consistência interna são inviáveis com avaliador único.
  \item \textbf{$N \geq 2$ avaliadores} (cenário ampliado, em prospecção
        ativa antes da defesa): mantém-se a entrevista qualitativa pelas
        quatro dimensões e, adicionalmente, aplicam-se os itens Likert do
        TAM-AIN com pontuação descritiva por dimensão (média e amplitude
        sobre os avaliadores). Não se reivindica validade psicométrica
        plena --- $N$ pequeno não autoriza análise fatorial --- mas a
        agregação descritiva passa a ser tecnicamente defensável como
        evidência exploratória.
\end{itemize}

\textbf{Em qualquer dos cenários}, o instrumento psicométrico completo
(análise fatorial confirmatória, $\alpha$ de Cronbach, validade convergente
e discriminante) permanece como referência para o piloto multi-avaliador
estruturado listado em Trabalhos Futuros (TF6), que exige amostra
populacional e aprovação de Comitê de Ética em Pesquisa.

\subsection{Perfil do Especialista}

\aluno{Caracterizar o expert: área de atuação (enfermagem em saúde pública, atenção
primária), tempo de experiência no manejo da ILTB, vínculo institucional (anonimizar
se necessário), formação. Incluir Termo de Consentimento Livre e Esclarecido
assinado.}

\subsection{Protocolo da Sessão}

A sessão foi estruturada em quatro etapas, alinhadas às atividades de
\textit{Demonstração} e \textit{Avaliação} do DSRM \cite{peffers2007}:

\begin{enumerate}[label=\textbf{(\arabic*)}]
  \item \textbf{Abertura e consentimento:} assinatura do TCLE, apresentação dos
        objetivos do chatbot, do escopo do corpus e dos limites declarados da
        ferramenta (apoio à decisão, não substituição do julgamento clínico).
  \item \textbf{Demonstração funcional:} apresentação das principais funcionalidades
        da interface, com exemplos previamente preparados, para que o especialista
        compreenda o fluxo de interação antes da fase avaliativa.
  \item \textbf{Interação com cenários clínicos:} o especialista submete consultas
        ao chatbot com base em perguntas reais que recebe na prática
        (rastreamento de PVHIV, esquemas terapêuticos, populações especiais,
        interações medicamentosas). Em paralelo, são apresentadas amostras das 38
        questões já avaliadas pelo RAGAS para revisão dirigida.
  \item \textbf{\textit{Debriefing} estruturado:} entrevista semi-estruturada
        orientada por instrumentos validados, descritos a seguir.
\end{enumerate}

Em conformidade com a política de privacidade descrita na
Seção~\ref{sec:lgpd-ref}, a sessão adota retenção zero do histórico de conversa,
\textit{logging} mínimo e ausência de dados identificáveis nas consultas.

\subsection{Instrumentos de Avaliação}

A avaliação combina quatro instrumentos, conforme recomendado para validação
multidimensional de chatbots clínicos com avaliador único \cite{ocaf2024}:

\begin{enumerate}[label=\textbf{I\arabic*}]
  \item \textbf{Acurácia técnica contra \textit{ground truth} do Ministério da Saúde:}
        o especialista classifica cada resposta como \textit{correta},
        \textit{parcialmente correta} ou \textit{incorreta} em relação ao protocolo
        oficial \cite{brasil2022}, indicando o trecho conflitante quando aplicável.
  \item \textbf{Classificação ordinal de severidade do erro:} escala Likert de cinco
        pontos (sem risco, risco baixo, risco moderado, risco alto, conduta
        perigosa) aplicada a cada resposta incorreta ou parcialmente correta,
        permitindo análise de segurança independente da acurácia bruta.
  \item \textbf{Tríade de qualidade RAG (rubrica qualitativa):} para cada resposta, o
        especialista avalia separadamente \textit{fidelidade} (a resposta deriva do
        contexto recuperado?), \textit{relevância da resposta} (responde à pergunta?)
        e \textit{relevância do contexto} (o trecho recuperado é o correto do
        protocolo?), seguindo a estrutura proposta por \citeauthor{es2024}
        \cite{es2024}.
  \item \textbf{Entrevista semi-estruturada pelas dimensões do TAM-AIN
        $+$ (condicional ao $N$) itens Likert do instrumento:} roteiro
        qualitativo organizado pelas quatro dimensões do \textit{Technology
        Acceptance Model for AI in Nursing} \cite{nursingTAM2024} ---
        alinhamento ético, prontidão organizacional, preservação da
        identidade profissional e capacidade de infraestrutura ---, com
        perguntas abertas sobre barreiras e facilitadores percebidos para
        adoção no SUS. Conforme discutido na nota introdutória desta seção,
        no cenário $N=1$ a coleta é narrativa por dimensão (sem agregação);
        no cenário $N \geq 2$, aplicam-se adicionalmente os itens Likert do
        instrumento com agregação descritiva (média e amplitude por
        dimensão), sem reivindicação de validade psicométrica formal.
\end{enumerate}

A escolha por um instrumental multimétrico em detrimento de estatísticas tradicionais
de concordância inter-avaliador (e.g., coeficiente Kappa \cite{landis1977}) decorre
da natureza da amostra: com $N=1$, instrumentos como Kappa de Cohen perdem aderência
metodológica, pois exigem ao menos dois avaliadores para o cálculo da concordância
acima do acaso. A literatura defende, nesses cenários, o deslocamento do esforço
analítico para validade de conteúdo e segurança clínica \cite{ocaf2024}.

\subsection{Resultados e Análise da Sessão}

\aluno{Apresentar: número de questões avaliadas, distribuição de respostas por
categoria (correta / parcialmente correta / incorreta), severidade média dos erros,
síntese narrativa por dimensão da entrevista TAM-AIN (alinhamento ético,
prontidão organizacional, identidade profissional, infraestrutura), exemplos
representativos de respostas avaliadas positiva e negativamente, observações
qualitativas do especialista. Não reportar \emph{scores} numéricos TAM-AIN ---
em $N=1$ o instrumento não suporta esse tipo de agregação.}

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Discussão}
\label{cap:discussao}
% ─────────────────────────────────────────────────────────────────────────────

\section{Interpretação dos Resultados}

Os resultados consolidados da avaliação RAGAS, à luz da ablação $2 \times 2$
descrita na Seção~\ref{sec:ablacao-2x2} e da configuração final pós-migração
da Seção~\ref{sec:gate-pos-migracao}, comportam três leituras complementares.

\textbf{Primeira: o embedding era o gargalo dominante.} A migração do
embedding inicial de 384 dimensões (MiniLM, da família SBERT
\cite{reimers2019}) para o BGE-M3 1024D produziu ganhos de $+25$ a
$+45$\,pp em todas as métricas para os dois geradores avaliados, com
magnitudes várias vezes superiores ao DP inter-seed
(Tabela~\ref{tab:ablacao-2x2}). Esse
achado é convergente com os \textit{benchmarks} multilíngues
\cite{SHTI2023, mmteb2025} e com a revisão sistemática de
\citeauthor{ocaf2024} \cite{ocaf2024}, que identifica a escolha do
embedding como o principal lever de qualidade em RAG clínico. O
patamar atingido de \textit{Context Precision} ($0{,}956 \pm 0{,}009$ na
configuração final) ultrapassa confortavelmente a meta de $0{,}75$, validando
empiricamente a hipótese predita pela literatura para o contexto deste trabalho.

\textbf{Segunda: modelo maior não dissolve o gap.} A substituição do
Llama~3.3~70B pelo GPT-4o, mantendo o restante do pipeline constante,
\emph{piorou} a \textit{Faithfulness} em ambas as configurações de
embedding ($0{,}486 \to 0{,}395$ no MiniLM; $0{,}679 \to 0{,}620$ no
BGE-M3, valores médios sobre 3 seeds, com SDs reportados na
Tabela~\ref{tab:ablacao-2x2}). O GPT-4o adere mais estritamente às instruções do \textit{system
prompt} e produz respostas de fallback (recusa) com maior frequência,
comportamento que o RAGAS pontua como menos fiel por ausência de claims
verificáveis. Achados análogos em RAG clínico são descritos por
\citeauthor{zakka2024almanac} \cite{zakka2024almanac} no sistema Almanac
(NEJM AI). Este paradoxo tem duas implicações: (i) a escolha do Llama~3.3~70B
como gerador do artefato é tecnicamente justificada pelos próprios números, não
apenas por considerações de custo (Groq oferece o modelo a custo zero no
free tier); (ii) métricas automatizadas como o RAGAS exigem
complementação por avaliação humana \cite{ocaf2024, gallifant2025tripod}, pois
podem penalizar comportamento desejável do ponto de vista de segurança
clínica.

\textbf{Terceira: intervenções pós-recuperação fecharam $\sim 85\%$ do gap
residual de \textit{Faithfulness}.} A ablação $2 \times 2$ identificou que o
retriever era o gargalo dominante, mas a migração para BGE-M3 ainda
deixou \textit{Faithfulness} em $0{,}679 \pm 0{,}004$. Duas iterações
subsequentes, fundamentadas na literatura específica e descritas nas
Seções~\ref{sec:hybrid}--\ref{sec:gate-final}, elevaram esse patamar para
$0{,}752 \pm 0{,}004$. A reanálise com 3 seeds revelou que a divisão de
contribuição entre as iterações é diferente do reportado em corridas únicas:
(i) busca híbrida BM25 + denso com \textit{Reciprocal Rank Fusion}
\cite{cormack2009, formal2021, khattab2020} produziu o \textbf{salto dominante
em \textit{Faithfulness}} ($\Delta = +0{,}061$, $+9{,}0\%$, magnitude
$\approx 3{,}6\times$ o DP máximo do par), além de $+0{,}064$ em
\textit{Context Recall};
(ii) reranking cross-encoder mGTE
\cite{zhang2024mgte, wang2024bestpractices} contribuiu modestamente em
\textit{Faithfulness} ($+0{,}012$, $+1{,}6\%$, dentro do envelope de
incerteza), mas \textbf{restaurou \textit{Context Precision} a $0{,}956
\pm 0{,}009$} ($+0{,}041$ sobre o híbrido sem rerank) e
\textbf{reduziu a variância inter-seed} de \textit{Faithfulness} de
$\pm 0{,}017$ para $\pm 0{,}004$, indicando função estabilizadora; (iii) teste
de \textit{chunk\_size} reduzido sem efeito mensurável
(Seção~\ref{sec:chunk-test}), limitado pela estrutura hierárquica do
\textit{chunker}. O gap residual final de $\sim 0{,}048$ converge ao patamar
descrito por \citeauthor{ocaf2024} \cite{ocaf2024} como teto típico antes de
intervenções de self-reflection \cite{jeong2024selfbiorag}, registrada
como linha de pesquisa imediata no Capítulo~\ref{cap:conclusao}.

A leitura conjunta dos quatro scores é coerente com o cenário previsto
pela revisão sistemática de \citeauthor{ocaf2024} \cite{ocaf2024}: sistemas
RAG biomédicos obtêm ganho médio de $1{,}35\times$ em eficácia clínica sobre
LLMs sem recuperação, mas atingem um teto comportamental quando a métrica
exige fidelidade verbatim a um modelo abstrativo. A resposta metodológica
adequada não é forçar o LLM a respostas extrativas (que diminuiria a utilidade
clínica em consultas que exigem síntese), mas combinar duas estratégias
complementares: (i) avançar tecnicamente via intervenções pós-recuperação
referenciadas acima; (ii) complementar a avaliação com instrumento humano
estruturado, conforme detalhado na Seção~\ref{sec:sessao-expert}.

\section{Limitações}

\begin{enumerate}[label=\textbf{L\arabic*}]
  \item Corpus limitado a documentos públicos --- nenhum dado clínico real de
        pacientes foi utilizado;
  \item Ausência de \textit{dataset} clínico anotado em PT-BR para validação
        RAGAS independente;
  \item Avaliação qualitativa com amostra reduzida (1 expert);
  \item Dependência de API externa para o LLM (Groq) --- a operação do
        free tier (limite de 100K tokens diários, TPD) restringe
        ciclos experimentais; o presente trabalho recorreu ao plano
        \textit{Dev Tier} pago para concluir a célula Llama+BGE-M3 da
        ablação $2 \times 2$ (Seção~\ref{sec:ablacao-2x2});
  \item Pipeline atual incorpora busca híbrida BM25+denso e
        reranking cross-encoder mGTE (Seções
        \ref{sec:hybrid} e \ref{sec:rerank}), mas não inclui
        self-reflection sobre a resposta gerada
        \cite{jeong2024selfbiorag}. A literatura reporta ganho típico de
        $+8$ a $+15$\,pp em \textit{Faithfulness} para essa intervenção, mas
        o achado paradoxal da Seção~\ref{sec:ablacao-2x2} (modelos mais
        cautelosos pontuam pior em \textit{Faithfulness} no RAGAS) torna
        essa expectativa condicional: \textit{self-reflection} pode
        regredir a métrica apesar de melhorar a segurança clínica real,
        exigindo co-design com revisão da métrica de avaliação (TF1).
        \textit{Chunking} baseado em tokens (\textit{e.g.}, Small-to-Big) é
        intervenção complementar potencial não testada
        \cite{advancedChunkingRAG2025};
  \item \textbf{Viés de construção do \textit{dataset} de avaliação.}
        As 38 questões \textit{in-scope}, seus \textit{ground truths}
        e o corpus indexado foram preparados pelo mesmo autor, a partir
        dos mesmos documentos oficiais. Os \textit{ground truths} são
        trechos extraídos verbatim do corpus, e as métricas RAGAS verificam
        claims da resposta contra o contexto recuperado desse
        mesmo corpus. Há, portanto, três efeitos correlacionados que
        favorecem o sistema em relação a um cenário de uso clínico real:
        (i) recuperabilidade garantida por construção (eventuais falhas
        de \textit{Context Recall} indicam problemas de retriever, não
        ausência de informação na base, o que é uma propriedade desejável
        para auditoria mas otimista para benchmark); (ii) ausência de
        questões \textit{ambíguas} ou \textit{mal-formuladas} típicas da
        prática (o autor formulou perguntas inspiradas em consultas reais,
        mas com clareza típica de teste); (iii) o LLM-juiz
        (\texttt{gpt-4o-mini}) pertence à mesma família comercial de um
        dos candidatos avaliados na ablação $2\times2$ (GPT-4o), o que
        introduz a possibilidade de \emph{viés intra-família} a favor desse
        candidato --- ainda que o efeito observado tenha sido o oposto
        (Seção~\ref{sec:ablacao-2x2}), o risco existe e é reconhecido na
        literatura \cite{es2024, ocaf2024}. Mitigação parcial adotada:
        validação cruzada por especialista (Seção~\ref{sec:sessao-expert})
        sobre subconjunto das 38 questões. Mitigação completa --- dataset
        formulado por terceiros, juiz humano ou de família independente,
        e \emph{benchmark} público em PT-BR --- está listada em Trabalhos
        Futuros (TF8);
  \item \textbf{Cobertura assimétrica de replicação multi-seed.} As
        Tabelas~\ref{tab:ablacao-2x2} e~\ref{tab:hybrid-rerank} reportam
        médias e desvios padrão sobre \textbf{três seeds independentes} por
        célula --- coleta nova de respostas do gerador (Llama com
        temperatura $0{,}1$ / GPT-4o com temperatura $0$, conforme
        \texttt{app/src/llm/client.py}) e nova execução RAGAS pelo juiz
        \texttt{gpt-4o-mini} (temperatura $0$). As demais tabelas do
        Capítulo~\ref{cap:avaliacao} (Tabela~\ref{tab:ragas} de marcos de
        desenvolvimento, Seção~\ref{sec:chunk-test} sobre tuning de
        \textit{chunk\_size}) reportam corridas únicas e não admitem
        intervalo de confiança formal. A escolha foi deliberada: replicação
        multi-seed concentrou-se nas comparações comparativas que sustentam
        as inferências centrais do trabalho (ablação $2 \times 2$ e
        iterações pós-recuperação); marcos de desenvolvimento histórico e
        ajustes pontuais não suportariam ganho informativo proporcional ao
        custo de re-execução.
\end{enumerate}

\section{Compatibilidade do Desenho com Requisitos LGPD em POC}

Esta seção discute em que medida o desenho técnico adotado neste trabalho é
\emph{compatível} com os requisitos da Lei Geral de Proteção de Dados Pessoais
(LGPD, Lei n\textordmasculine\ 13.709/2018) no contexto restrito de uma prova
de conceito (POC) acadêmica. \textbf{Não se reivindica \emph{conformidade}
plena} --- termo que, em sentido jurídico-regulatório, exigiria Relatório de
Impacto à Proteção de Dados Pessoais (RIPD/DPIA), Acordo de Tratamento de
Dados (DPA) com operadores e fluxo auditável de consentimento, instrumentos
que não foram produzidos no escopo deste TCC. A reivindicação defensável é a
de que o desenho atual \emph{evita o escopo da LGPD} pela natureza dos dados
manipulados, e que adequações ativas serão necessárias antes de qualquer
implantação institucional.

\textbf{Por que o desenho é compatível em cenário de POC.} O corpus é composto
exclusivamente de documentos públicos do Ministério da Saúde e da OMS --- não
há dado pessoal de paciente indexado. As interações com o chatbot, no protocolo
da avaliação com especialista (Seção~\ref{sec:sessao-expert}), adotam retenção
zero do histórico de conversa, \textit{logging} mínimo e ausência de dados
identificáveis nas consultas. O pipeline de embeddings executa
100\% localmente, de modo que as consultas formuladas pelo especialista durante
a sessão não transitam por APIs de terceiros antes da busca vetorial. O LLM
gerador (Llama~3.3~70B via Groq) é acessado sob política declarada de não
retenção de prompts, o que reduz --- mas não elimina --- o risco de
exposição a operador externo \cite{lgpdSaude2023, sokPrivacyLLM2026}.

\textbf{Por que não se reivindica conformidade plena.} A LGPD aplica-se a
qualquer tratamento de dados pessoais, e a interface coleta perguntas
livremente formuladas pelo usuário. Mesmo com corpus público e API sem
armazenamento, o conteúdo dessas perguntas pode trazer informações
clinicamente identificáveis (descrição de caso, datas, comorbidades,
identificadores indiretos), conforme caracterizam
\citeauthor{privacyRAGHealthcare2025} \cite{privacyRAGHealthcare2025} ao
classificar o profissional clínico como \emph{adversário interno acidental}.
\textbf{O desenho atual não inclui as mitigações ativas exigidas por uma
conformidade institucional plena}: não há \emph{scrubbing} ex-ante de
identificadores no \textit{frontend}, não há \textit{logging} consciente de
privacidade com pseudonimização de sessão, não há DPA assinado com o operador
do LLM, não há RIPD/DPIA submetido à ANPD ou ao Comitê de Ética em Pesquisa.
Esses instrumentos são pré-requisitos de implantação real, não da POC.

\textbf{Adequações necessárias para implantação institucional.} Em cenários
de implantação institucional --- ainda fora do escopo deste trabalho, mas
previstos como continuidade natural ---, três pontos de atenção emergem da
literatura.

O primeiro é a \textbf{entrada de dados identificáveis pelo usuário final}. Mesmo com
corpus público e API sem armazenamento, o conteúdo das consultas formuladas pelo
profissional pode trazer informações clinicamente identificáveis (descrição de caso,
data, comorbidades, identificadores indiretos). \citeauthor{privacyRAGHealthcare2025}
\cite{privacyRAGHealthcare2025} categorizam o pesquisador e o profissional clínico
como \textit{adversários internos acidentais}: agentes legítimos cujo fluxo de
trabalho pode causar vazamento inadvertido. Para mitigar esse risco, recomenda-se
implementar \textit{scrubbing} ex-ante no \textit{frontend} --- redação automática de
identificadores antes do envio ao retriever --- e exibir aviso explícito ao usuário
sobre a política de tratamento de dados sensíveis.

O segundo é a \textbf{rastreabilidade auditável}. Sistemas de IA em saúde sob a LGPD
demandam \textit{logging} compatível com auditoria sem violar a finalidade declarada
do tratamento \cite{lgpdSaude2023, sokPrivacyLLM2026}. Em produção, isso implica
registrar metadados de uso (timestamps, tipo de consulta, presença de
\textit{disclaimer}, identificador de sessão pseudonimizado) sem reter o conteúdo das
consultas, mecanismo descrito como \textit{logging consciente de privacidade}
\cite{privacyRAGHealthcare2025}. A retenção zero adotada nesta POC não escala para um
serviço institucional, que precisa demonstrar rastreabilidade frente a eventuais
incidentes.

O terceiro é a \textbf{relação jurídica com o provedor do LLM}. O modelo Llama~3.3
70B foi acessado neste trabalho via API Groq, sem armazenamento declarado de prompts;
em ambiente institucional, contudo, recomenda-se a celebração de Acordo de
Tratamento de Dados (DPA, na sigla em inglês) com o operador, em conformidade com os
artigos 39 e 47 da LGPD, ou o emprego de \textit{inference} local em GPU dedicada
para eliminar a transferência. A literatura específica sobre RAG biomédico endossa
essa estratégia, observando que adversários do lado da nuvem podem reconstruir
trechos sensíveis a partir de ativações intermediárias mesmo quando o provedor não
armazena prompts explicitamente \cite{privacyRAGHealthcare2025, sokPrivacyLLM2026}.

Em síntese, o desenho atual é \emph{compatível} com requisitos da LGPD no
recorte de POC porque \textbf{evita} o escopo da lei (corpus público,
embeddings locais, retenção zero, ausência de dado identificável),
e não porque implementa mecanismos ativos de proteção exigidos em ambiente
institucional. A reivindicação de \textbf{conformidade plena} pressuporia
RIPD/DPIA, DPA com operador, \emph{scrubbing} ex-ante de identificadores e
\textit{logging} auditável pseudonimizado --- instrumentos não produzidos no
escopo deste TCC e contemplados como linha de pesquisa subsequente
(Capítulo~\ref{cap:conclusao}, TF9).

\section{Implicações para Adoção}

A adoção efetiva de chatbots clínicos como o desenvolvido neste trabalho depende de
fatores que ultrapassam a qualidade técnica do artefato. As quatro dimensões do
TAM-AIN \cite{nursingTAM2024}, utilizadas como eixos do roteiro de entrevista na
avaliação com especialista (Seção~\ref{sec:sessao-expert}), apontam fatores
críticos que devem ser ponderados em qualquer estratégia de implantação no
SUS --- e que justificam, em piloto multi-avaliador subsequente, a aplicação do
instrumento psicométrico completo (Trabalhos Futuros, TF6).

\textbf{Alinhamento ético e confiança.} A literatura indica padrão de
\textit{otimismo cauteloso} entre profissionais de enfermagem frente à IA: cerca de
85\% reconhecem benefícios potenciais para precisão diagnóstica e redução de erro
humano, mas aproximadamente 70\% mantêm reservas quanto ao componente humano do
cuidado e à segurança dos dados \cite{nursingTAM2024}. Para o chatbot ILTB, isso
implica investir em comunicação clara dos limites da ferramenta e da política de
privacidade --- sob pena de minar a base de confiança necessária para uso continuado.

\textbf{Preservação da identidade profissional.} O TAM-AIN distingue ``ferramenta de
apoio'' de ``substituto da decisão clínica'', dimensão particularmente sensível
entre profissionais com mais tempo de prática, que tendem a manifestar receio de
desqualificação. A interface da POC já incorpora \textit{disclaimer} explícito de
não substituição do julgamento profissional, mas a literatura sugere que a aderência
de longo prazo exige que o sistema demonstre, no fluxo da interação, capacidade de
reforçar o protagonismo do enfermeiro --- por exemplo, citando fonte e contexto a
cada resposta para que o profissional valide ativamente as informações.

\textbf{Prontidão organizacional e infraestrutura.} A heterogeneidade do parque
tecnológico das unidades de atenção primária do SUS configura barreira
infraestrutural concreta: conexões intermitentes, navegadores desatualizados, falta
de dispositivos dedicados \cite{nursingTAM2024}. A arquitetura web atual, leve e
sem dependência de instalação local no \textit{client}, é compatível com esse cenário,
mas estudos futuros precisarão verificar latência percebida em redes de menor
qualidade e estratégias de \textit{caching} para uso \textit{offline} parcial.

\textbf{Carga de trabalho e usabilidade percebida.} Entre as barreiras mais
reportadas pela literatura estão a sobrecarga e o ceticismo sobre o tempo
adicional exigido para incorporar novas ferramentas ao fluxo. Avaliações
subsequentes deverão medir, além da acurácia clínica das respostas, o ganho líquido
de tempo na consulta a protocolos --- métrica operacional que sustenta o argumento
de utilidade frente ao gestor da unidade.

Esses fatores indicam que a transição da POC para implantação institucional não
constitui apenas tarefa de engenharia (resolvendo as questões discutidas na
Seção~\ref{cap:discussao}), mas também tarefa organizacional, dependente de adesão
da liderança local e de capacitação dos profissionais.

% ─────────────────────────────────────────────────────────────────────────────
\chapter{Conclusão}
\label{cap:conclusao}
% ─────────────────────────────────────────────────────────────────────────────

Este trabalho desenvolveu e avaliou um chatbot de apoio clínico ao manejo da
Infecção Latente por \textit{Mycobacterium tuberculosis}, implementado com a
arquitetura RAG e documentos oficiais em língua portuguesa, seguindo a
metodologia DSRM \cite{peffers2007}. O artefato é composto por um pipeline
local de embeddings multilíngues, um vector store ChromaDB com
indexação HNSW e o modelo Llama~3.3~70B acessado via API Groq como gerador, e
foi disponibilizado por meio de uma interface web servida pelo FastAPI.

A avaliação automatizada com o framework RAGAS sobre 38 questões
clínicas foi conduzida em três etapas. A configuração inicial, com embedding
384D e o Llama~3.3~70B como gerador, atingiu \textit{Context Precision} de
$0{,}736 \pm 0{,}007$ e \textit{Faithfulness} de $0{,}486 \pm 0{,}030$
(célula Llama+MiniLM da ablação, 3 seeds) --- patamar consistente com a
literatura de RAG biomédico, mas insuficiente para a meta de $0{,}80$. Uma
ablação $2 \times 2$ cruzando gerador (Llama vs GPT-4o) e embedding
(MiniLM 384D vs BGE-M3 1024D), com 3 seeds por célula, isolou o embedding
como gargalo dominante; a migração para \texttt{BAAI/bge-m3} elevou todas as
métricas a um patamar intermediário com \textit{Faithfulness} de
$0{,}679 \pm 0{,}004$. Duas iterações pós-recuperação subsequentes ---
busca híbrida BM25 + denso com \textit{Reciprocal Rank Fusion} (responsável
pelo salto dominante em \textit{Faithfulness}, $+0{,}061$) e reranking
cross-encoder mGTE (responsável por consolidar \textit{Context Precision}
acima de $0{,}95$ e por reduzir variância inter-seed) --- consolidaram o
gate operacional final do artefato (também avaliado em 3 seeds). O principal
indicador de qualidade alcançado é a
\textbf{\textit{Context Precision} de $0{,}956 \pm 0{,}009$}, que representa
\emph{PASS robusto} sobre a meta de $0{,}75$ (margem absoluta de $+0{,}206$
sobre o alvo; intervalo observado entre as 3 seeds, $[0{,}949;\, 0{,}966]$,
permanece integralmente acima da meta) e ultrapassa em $+30\%$ a célula
Llama+MiniLM da ablação, evidenciando que a etapa de recuperação convergiu
para um patamar maduro.
\textit{Answer Relevancy} $0{,}725 \pm 0{,}020$ e \textit{Context Recall}
$0{,}814 \pm 0{,}003$ completam o quadro. A métrica de \textit{Faithfulness}
atingiu $0{,}752 \pm 0{,}004$ --- a $\sim 0{,}048$ da meta original de
$0{,}80$ ---, mas sua leitura exige cautela: a ablação $2 \times 2$ mostrou
que substituir o Llama pelo GPT-4o \emph{piora} essa métrica em ambos os
embeddings, evidenciando que o RAGAS, na sua formulação atual,
penaliza comportamento conservador clinicamente desejável. Por essa razão,
adota-se \textit{Context Precision} como indicador-síntese do gate final
deste trabalho, e \textit{Faithfulness} como métrica auxiliar lida em
conjunto com o paradoxo abstrativo-vs-conservador discutido no
Capítulo~\ref{cap:discussao}.
Esse paradoxo, convergente com observações independentes em RAG clínico
\cite{zakka2024almanac}, justifica tecnicamente a escolha do Llama como gerador
do artefato e a adoção de \textit{Context Precision} como indicador-síntese.
\aluno{Completar com 2--3 frases sobre o resultado da sessão estruturada
com o especialista (acurácia clínica média, severidade modal dos erros, síntese
narrativa por dimensão TAM-AIN --- alinhamento ético, prontidão, identidade
profissional, infraestrutura --- e principais observações qualitativas) após a
execução da Seção~\ref{sec:sessao-expert}.}

As principais contribuições são o artefato aberto, o \textit{dataset} de 38
questões clínicas em português sobre ILTB com \textit{ground truths} extraídos
verbatim dos protocolos, a análise empírica do \textit{trade-off} LGPD vs.
desempenho e a proposta de protocolo de avaliação multidimensional com $N=1$
combinando RAGAS, rubrica clínica e entrevista qualitativa pelas dimensões do
TAM-AIN. \aluno{Ao finalizar, ajustar este
parágrafo se a sessão com expert produzir contribuição empírica adicional.}

\section{Trabalhos Futuros}
\label{sec:trabalhos-futuros}

A avaliação experimental e a revisão bibliográfica conduzidas no escopo deste
trabalho identificaram um conjunto de direções de evolução, agrupadas em
três frentes. A frente técnica foi parcialmente endereçada no próprio
desenvolvimento do artefato (Seções \ref{sec:hybrid} e \ref{sec:rerank}, com
ganho cumulativo de $+0{,}073$ em \textit{Faithfulness} sobre a célula
Llama+BGE-M3 denso); restam linhas de pesquisa para fechar o gap residual final
($0{,}752 \pm 0{,}004 \to \geq 0{,}80$).

\textbf{Frente técnica:}

\begin{enumerate}[label=\textbf{TF\arabic*}]
  \item \textbf{Self-RAG / self-reflection com reformulação da métrica de
        avaliação.} A literatura reporta ganho de $+8$ a $+15$ pontos em
        \textit{Faithfulness} em QA biomédico \textit{long-form} com
        \textit{self-reflection} \cite{jeong2024selfbiorag}, ao custo de
        aproximadamente dobrar o número de chamadas ao LLM por consulta.
        Esta seria, em princípio, a intervenção mais promissora para fechar
        o gap residual de $\sim 0{,}048$ até a meta de $0{,}80$ (calculado
        sobre o gate final $0{,}752 \pm 0{,}004$). \textbf{Contudo, o
        achado paradoxal da Seção~\ref{sec:ablacao-2x2} indica uma tensão
        não trivial}: substituir o Llama por GPT-4o (mais aderente a
        instruções de \textit{fallback}, comportamento próximo ao que
        \textit{self-reflection} induz) \emph{piora} a
        \textit{Faithfulness} no RAGAS. Se um \textit{critic step} torna o
        gerador mais cauteloso --- e \textit{Self-RAG} é exatamente isso ---
        é plausível que a métrica RAGAS \emph{regrida} apesar de a
        segurança clínica real melhorar. Portanto, esta linha de pesquisa
        deve ser conduzida em \textbf{co-design com a revisão da métrica de
        avaliação} (e.g., \textit{Faithfulness} ponderada por relevância
        clínica, ou substituição por rubrica multidimensional avaliada por
        especialista), e não como substituição plug-and-play do gate atual;
  \item \textbf{Chunking estrito por tokens do modelo} ---
        substituir o \textit{chunker} hierárquico atual (guiado por
        cabeçalhos do \textit{Markdown}) por estratégia tipo
        Small-to-Big, Sliding Window ou proposições
        atômicas (DenseX) \cite{advancedChunkingRAG2025, gao2024},
        permitindo o efeito de chunks de 300--500 tokens
        previsto por \citeauthor{wang2024bestpractices}
        \cite{wang2024bestpractices} mas não capturado no teste descrito na
        Seção~\ref{sec:chunk-test};
  \item \textbf{Prompt v5 com citação granular de span} ---
        instruir o gerador a citar não apenas o documento, mas o trecho
        literal de cada claim. \citeauthor{zakka2024almanac}
        \cite{zakka2024almanac} usa essa estratégia no sistema Almanac;
        ganho esperado em fidelidade, ao risco de intensificar
        comportamento conservador do gerador (a história de prompts v2--v4
        descrita no Apêndice~\ref{ape:prompt} mostra que prompts mais
        restritivos podem piorar a métrica RAGAS);
  \item \textbf{LLM especializado em diretrizes brasileiras} --- avaliar a
        adoção de um modelo \textit{fine-tuned} em PCDTs do SUS (e.g.,
        Qwen2.5-14B-PCDT, \citeauthor{teachingLLMsBrazil2026},
        \citeyear{teachingLLMsBrazil2026}) em substituição ao gerador
        generalista atual. Estudos recentes em saúde pública brasileira
        mostram que modelos com injeção de conhecimento dos PCDTs superam
        \textit{RAG} \textit{web-grounded} em acurácia
        (\textit{e.g.}, HealthBench-BR $83{,}9\%$); a contrapartida é o
        custo de fine-tuning e a perda de atualização em tempo
        real \cite{teachingLLMsBrazil2026};
  \item \textbf{Simplificação arquitetural do vector store} ---
        avaliar empiricamente se a substituição do ChromaDB por FAISS local
        ou índice em memória (\textit{e.g.}, \texttt{numpy.array}) sobre
        o corpus de 898 chunks introduz regressão mensurável de
        \textit{recall}/latência. A literatura sugere que, para corpora
        pequenos ($< 10^4$ chunks), a busca exaustiva
        ($O(N)$) é computacionalmente equivalente ao HNSW logarítmico
        \cite{ocaf2024}; eliminar a dependência do ChromaDB simplificaria
        \textit{deploy} em \textit{hardware} restrito da APS.
\end{enumerate}

\textit{Já incorporadas no escopo deste trabalho:} migração de
embedding para BGE-M3 1024D (Seção~\ref{sec:gate-pos-migracao}),
busca híbrida BM25 + denso com RRF (Seção~\ref{sec:hybrid}) e
reranking cross-encoder mGTE (Seção~\ref{sec:rerank}).

\textbf{Frente metodológica:}

\begin{enumerate}[label=\textbf{TF\arabic*}, start=6]
  \item \textbf{Piloto clínico com múltiplos profissionais de enfermagem} ---
        com aprovação de Comitê de Ética em Pesquisa (CEP), permitindo
        cálculo de concordância inter-avaliador (e.g., Kappa
        \cite{landis1977}) e aplicação do TAM-AIN como instrumento
        psicométrico pontuável \cite{nursingTAM2024}, com análise fatorial
        confirmatória e $\alpha$ de Cronbach por dimensão --- procedimentos
        inviabilizados em $N=1$ e que neste trabalho foram substituídos por
        entrevista qualitativa pelas dimensões do modelo
        (Seção~\ref{sec:sessao-expert});
  \item \textbf{Estudo das barreiras operacionais da enfermagem brasileira
        no manejo da ILTB} --- a revisão bibliográfica conduzida nesta
        pesquisa identificou lacuna de literatura específica sobre
        barreiras operacionais (esquemas 6H/3HP/4R, rastreio em PVHIV,
        contactantes domiciliares) sob a perspectiva da enfermagem da
        atenção primária brasileira. Recomenda-se estudo qualitativo
        dedicado (entrevistas semi-estruturadas em UBSs do ES, MG ou SP)
        que complemente os dados gerais reportados pela OMS
        \cite{who2018, who2020consolidatedTB, who2023};
  \item \textbf{Construção de \textit{benchmark} público em português} ---
        consolidação do \textit{dataset} de 38 questões com revisão por painel
        de especialistas, em formato compatível com avaliações futuras de
        outros sistemas RAG clínicos em PT-BR.
\end{enumerate}

\textbf{Frente operacional:}

\begin{enumerate}[label=\textbf{TF\arabic*}, start=9]
  \item \textbf{Implantação institucional} --- servidor dedicado com HTTPS,
        autenticação, \textit{logging} consciente de privacidade e Acordo de
        Tratamento de Dados (DPA) com o operador do LLM, conforme discutido na
        Seção~\ref{cap:discussao};
  \item \textbf{Canais de acesso ampliados} --- integração com sistemas
        existentes do SUS (e.g., e-SUS APS) e canal via WhatsApp Business API,
        aderente à expectativa de uso real por enfermeiros em campo;
  \item \textbf{Inferência local em \textit{hardware} dedicado} --- migração do
        gerador para servidor com GPU própria, eliminando a transferência de
        prompts a operador externo e fechando integralmente o
        \textit{loop} de conformidade LGPD descrito no
        Capítulo~\ref{cap:discussao}.
\end{enumerate}

% ═════════════════════════════════════════════════════════════════════════════
% ELEMENTOS PÓS-TEXTUAIS
% ═════════════════════════════════════════════════════════════════════════════
\postextual

\bibliography{referencias}

% ─── Apêndice A — System Prompt ──────────────────────────────────────────────
\begin{apendicesenv}

\partapendices

\chapter{System Prompt de Produção (v1)}
\label{ape:prompt}

A versão~v1 do \textit{system prompt} foi a que produziu o melhor resultado de
\textit{Faithfulness} entre as quatro testadas
(v1~$=0{,}586$; v4~$=0{,}574$; v3~$=0{,}457$; v2~$=0{,}429$), e foi mantida como
configuração de produção. As versões alternativas v2 e v3 introduziam restrições
fortes (``EXCLUSIVAMENTE'', limite de quatro frases, citação obrigatória de
documento e seção) que paradoxalmente reduziram a métrica RAGAS, pois levavam o
modelo a fazer \textit{fallbacks} desnecessários ou a incluir referências
explícitas a fontes que não constavam dos chunks recuperados. A versão
v4, baseada em \textit{few-shot} com dois exemplos autocontidos, ficou ligeiramente
abaixo de v1. A análise detalhada dessas iterações consta no diário técnico do
projeto.

\begin{quote}
\begin{small}
\begin{verbatim}
Você é um assistente clínico especializado nos protocolos do
Ministério da Saúde para Infecção Latente pelo Mycobacterium
tuberculosis (ILTB).

Regras obrigatórias:
1. Responda SOMENTE com base nos trechos de protocolo fornecidos
   no contexto.
2. Se a informação não estiver no contexto, diga: "Não encontrei
   essa informação nos protocolos indexados. Consulte o Manual
   de Recomendações do MS."
3. Cite sempre a seção de origem da resposta.
4. Não faça diagnósticos nem prescrições — apenas forneça
   informação de protocolo.
5. Use linguagem técnica adequada para enfermeiros.
\end{verbatim}
\end{small}
\end{quote}

O contexto recuperado pelo retriever é injetado pelo \textit{user
message}, não pelo \textit{system prompt}, conforme implementação em
\texttt{app/src/llm/client.py}. Os chunks são apresentados ao modelo na
ordem decrescente de similaridade cossenoidal, com identificador de fonte
(documento e cadeia de cabeçalhos) anexado a cada um.
de demonstração.}

% ─── Apêndice B — Dataset de Avaliação ───────────────────────────────────────
\chapter{Dataset de Avaliação RAGAS}
\label{ape:dataset}

O \textit{dataset} de avaliação contém 42 questões (38 \textit{in-scope} + 4
\textit{out-of-scope}), com \textit{ground truths} extraídos verbatim dos
arquivos Markdown indexados a partir dos protocolos do MS e da OMS. A tabela
abaixo lista todas as questões, agrupadas por categoria
(ET, IND, PE, DI, MO, IT, EA, FE).

\setlength{\LTpre}{0pt}
\setlength{\LTpost}{0pt}
{\footnotesize
\begin{longtable}{@{}p{1.4cm}@{\hspace{1mm}}p{0.7cm}@{\hspace{1mm}}p{4.5cm}p{6.5cm}@{}}
\toprule
\textbf{ID} & \textbf{Cat.} & \textbf{Pergunta} & \textbf{\textit{Ground truth}} \\
\midrule
\endfirsthead
\multicolumn{4}{c}{\textit{(continuação da Tabela do Apêndice~\ref{ape:dataset})}} \\
\toprule
\textbf{ID} & \textbf{Cat.} & \textbf{Pergunta} & \textbf{\textit{Ground truth}} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\textit{Continua na próxima página}} \\
\endfoot
\endlastfoot
\input{_dataset_rows.tex}
\end{longtable}
}

% ─── Apêndice C — Breakdown por categoria ────────────────────────────────────
\chapter{Breakdown por Categoria do Gate Final}
\label{ape:per-category}

Esta tabela apresenta a decomposição das métricas RAGAS do gate final por
categoria temática do \textit{dataset} (Apêndice~\ref{ape:dataset}). Cada
linha agrega 3 seeds independentes sobre as $N$ questões da categoria; a
média e o desvio padrão são calculados sobre as médias intra-categoria de
cada seed.

\textbf{Leitura descritiva apenas.} Com $N=4$ a $7$ questões por categoria,
nenhum intervalo de confiança formal é defensável; valores devem ser lidos
como indicação de tendência intra-categoria, não como métrica comparativa
entre categorias. A tabela serve para identificar padrões qualitativos
úteis à interpretação do paradoxo abstrativo--vs--conservador da
Seção~\ref{sec:interp-faith}, não para sustentar inferências estatísticas.

\begin{table}[ht]
  \caption{Breakdown por categoria do gate final (3 seeds; média~$\pm$~DP;
           leitura descritiva apenas devido a $N$ pequeno por categoria)}
  \label{tab:per-category}
  \footnotesize
  \begin{tabularx}{\textwidth}{@{}Xcccccc@{}}
  \toprule
  \textbf{Categoria} & \textbf{N} &
  \textbf{Faith.} & \textbf{Ans. Rel.} & \textbf{Ctx. Prec.} & \textbf{Ctx. Rec.} \\
  \midrule
  Esquemas terapêuticos (ET)      & 7 & $0{,}644 \pm 0{,}004$ & $0{,}883 \pm 0{,}024$ & $1{,}000 \pm 0{,}000$ & $0{,}952 \pm 0{,}000$ \\
  Populações especiais (PE)       & 7 & $0{,}661 \pm 0{,}031$ & $0{,}647 \pm 0{,}071$ & $0{,}996 \pm 0{,}007$ & $0{,}762 \pm 0{,}082$ \\
  Interações medicamentosas (IT)  & 5 & $0{,}677 \pm 0{,}106$ & $0{,}687 \pm 0{,}004$ & $0{,}967 \pm 0{,}000$ & $0{,}900 \pm 0{,}058$ \\
  Monitoramento (MO)              & 5 & $0{,}720 \pm 0{,}093$ & $0{,}543 \pm 0{,}106$ & $0{,}935 \pm 0{,}045$ & $0{,}789 \pm 0{,}019$ \\
  Diagnóstico (DI)                & 5 & $0{,}764 \pm 0{,}077$ & $0{,}707 \pm 0{,}030$ & $0{,}983 \pm 0{,}000$ & $0{,}700 \pm 0{,}000$ \\
  Indicações de tratamento (IND)  & 5 & $0{,}820 \pm 0{,}067$ & $0{,}768 \pm 0{,}042$ & $1{,}000 \pm 0{,}000$ & $0{,}900 \pm 0{,}000$ \\
  Efeitos adversos (EA)           & 4 & $0{,}836 \pm 0{,}063$ & $0{,}844 \pm 0{,}024$ & $0{,}868 \pm 0{,}012$ & $0{,}667 \pm 0{,}000$ \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor. Categorias ordenadas por
            \textit{Faithfulness} crescente.}
\end{table}

\textbf{Leitura qualitativa.} A decomposição revela um padrão consistente
com o paradoxo discutido na Seção~\ref{sec:interp-faith}: categorias com
\textbf{alta densidade numérica} --- esquemas terapêuticos (doses, durações,
regimes), populações especiais (restrições e ajustes por subgrupo) e
interações medicamentosas (combinações e mecanismos) --- concentram os
três menores valores de \textit{Faithfulness} ($0{,}644$ a $0{,}677$),
\textbf{mesmo quando a Context Precision é próxima de $1{,}000$}.
Categorias com texto mais livre e menos quantitativo --- efeitos adversos
e indicações de tratamento --- apresentam \textit{Faithfulness} mais alta
($0{,}820$ a $0{,}836$). Em conjunto, esses dados sustentam empiricamente
a hipótese de que o RAGAS penaliza síntese/cálculo sobre números: o
gerador, ao agregar dose e duração de regimes em uma frase, perde
correspondência verbatim que a métrica exige, ainda que o conteúdo
clínico esteja correto. Esse achado fortalece a recomendação metodológica
do trabalho: \textit{Context Precision} como indicador-síntese e
\textit{Faithfulness} como métrica auxiliar lida em conjunto com a
distribuição categoria-por-categoria.

% ─── Apêndice D — Posicionamento frente ao estado da arte ────────────────────
\chapter{Posicionamento Frente ao Estado da Arte}
\label{ape:estado-arte}

Esta seção apresenta uma comparação aproximada entre os resultados deste
trabalho e três referenciais da literatura recente de RAG biomédico
mais frequentemente citados nesta monografia, com a finalidade de orientar
a leitura sobre a posição do artefato. \textbf{A comparação direta tem
limites severos} --- datasets, idiomas, juízes LLM e estratégias de
\textit{prompting} variam entre os estudos, e nenhum dos referenciais
publica métricas RAGAS exatamente equivalentes às reportadas aqui ---,
sendo apresentada como tabela exploratória e não como \textit{benchmark}
formal.

\begin{table}[!htbp]
  \caption{Posicionamento aproximado vs.\ estado da arte de RAG biomédico
           (comparação exploratória)}
  \label{tab:sota}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \begin{tabularx}{\textwidth}{@{}p{2.6cm}p{2.5cm}cXr@{}}
  \toprule
  \textbf{Sistema} & \textbf{Domínio} & \textbf{Idioma} &
  \textbf{Métrica reportada} & \textbf{Valor} \\
  \midrule
  \multicolumn{5}{@{}l}{\emph{Referenciais da literatura}} \\
  \midrule
  Almanac \cite{zakka2024almanac}
    & Medicina interna & EN
    & Aval.\ humana de fidelidade (4 médicos)
    & n.r.\textsuperscript{a} \\
  Self-BioRAG \cite{jeong2024selfbiorag}
    & QA biomédico \textit{long-form} & EN
    & \textit{Faithfulness}; ganho com \textit{self-reflection}
    & $+8$--$15$\,pp\textsuperscript{b} \\
  Meta-análise \cite{ocaf2024}
    & RAG biomédico (30 estudos) & Hetero.
    & Teto comum de \textit{Faithfulness}
    & $\sim 0{,}75$ \\
  \midrule
  \multicolumn{5}{@{}l}{\emph{Este trabalho (gate final, 3 seeds)}} \\
  \midrule
  Chatbot ILTB
    & ILTB / TB latente & PT-BR
    & RAGAS \textit{Context Precision}
    & $0{,}956 \pm 0{,}009$ \\
  Chatbot ILTB
    & ILTB / TB latente & PT-BR
    & RAGAS \textit{Faithfulness}
    & $0{,}752 \pm 0{,}004$ \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor a partir da literatura citada.
            \textsuperscript{a} Valor numérico não-comparável: avaliação
            humana, não automática. \textsuperscript{b} Ganho relativo
            sobre RAG sem \textit{self-reflection} no mesmo
            \textit{benchmark} do paper. \textbf{Ressalvas:} datasets,
            juízes e definições operacionais distintas dos adotados aqui;
            ler como ordem de grandeza, não como comparação controlada.
            Replicação dos referenciais sobre o \textit{dataset} ILTB
            PT-BR consta em Trabalhos Futuros.}
\end{table}

\textbf{Leitura.} O patamar de \textit{Faithfulness} $0{,}752 \pm 0{,}004$
deste trabalho está \emph{na fronteira} do que a meta-análise de
\citeauthor{ocaf2024} \cite{ocaf2024} reporta como teto comum
sem \textit{self-reflection} ($\sim 0{,}75$). \textit{Self-RAG}
\cite{jeong2024selfbiorag} é a única intervenção que a literatura associa
a ganhos consistentes acima desse patamar, mas, como discutido nas
Seções~\ref{sec:interp-faith} e na Conclusão (TF1), o achado paradoxal
deste trabalho impõe ressalva: \textit{Self-RAG} adiciona cautela ao
gerador --- e, no \emph{benchmark} interno deste trabalho, cautela
adicional regrediu o RAGAS. A direção da intervenção precisa ser
co-desenhada com revisão da métrica de avaliação.

% ─── Apêndice E — Checklist TRIPOD-LLM ──────────────────────────────────────
\chapter{Checklist TRIPOD-LLM (Adaptada)}
\label{ape:tripod}

A diretriz \textit{TRIPOD-LLM} \cite{gallifant2025tripod} (extensão do
TRIPOD para estudos clínicos com modelos de linguagem) é referenciada em
diversos pontos desta monografia como base para complementar a avaliação
automatizada com avaliação humana e para reporte de viés do juiz e do
\textit{dataset}. Este apêndice apresenta uma checklist adaptada
parcialmente preenchida, mapeando os principais itens da diretriz ao
texto da monografia. O preenchimento completo segue como recomendação
metodológica para versões pós-defesa do trabalho ou para publicação
derivada.

\bigskip

\setlength{\LTpre}{0pt}
\setlength{\LTpost}{0pt}
{\footnotesize
\begin{longtable}{@{}p{0.6cm}p{5cm}p{6cm}p{1.8cm}@{}}
\caption{Checklist TRIPOD-LLM adaptada (preenchimento parcial)}
\label{tab:tripod} \\
\toprule
\textbf{\#} & \textbf{Item} & \textbf{Mapeamento na monografia} & \textbf{Status} \\
\midrule
\endfirsthead
\multicolumn{4}{c}{\textit{(continuação da Tabela~\ref{tab:tripod})}} \\
\toprule
\textbf{\#} & \textbf{Item} & \textbf{Mapeamento na monografia} & \textbf{Status} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\textit{Continua na próxima página}} \\
\endfoot
\bottomrule
\endlastfoot
\multicolumn{4}{@{}l}{\emph{Título, resumo e introdução}} \\
\midrule
1 & Título identifica uso de LLM em saúde & Capa & Atendido \\
2 & Resumo estruturado: domínio, dataset, métricas & Resumo & Parcial \\
3 & Justificativa do uso de LLM (vs.\ alternativas) &
    Cap.~\ref{cap:introducao}; D3 do Quadro~\ref{quad:decisoes-tecnicas} & Atendido \\
4 & Objetivos específicos e mensuráveis &
    Seção \textit{Objetivos Específicos} (OE1--OE4) & Atendido \\
\midrule
\multicolumn{4}{@{}l}{\emph{Métodos --- fonte de dados e população}} \\
\midrule
5 & Origem do corpus e processo de seleção &
    Cap.~\ref{cap:desenvolvimento}; Tabela~\ref{tab:corpus} & Atendido \\
6 & População alvo (perfil do usuário) &
    Seções~\ref{sec:adocao-ref}, \ref{sec:sessao-expert} & Atendido \\
7 & Construção do \textit{dataset} de avaliação &
    Seção \textit{Dataset de Avaliação} & Atendido \\
8 & Viés de construção do \textit{dataset} reconhecido &
    Limitação L6 & Atendido \\
\midrule
\multicolumn{4}{@{}l}{\emph{Métodos --- modelo, \emph{prompt}, parametrização}} \\
\midrule
9 & Modelo LLM identificado (versão, provedor) &
    D3 (Quadro~\ref{quad:decisoes-tecnicas}); Cap.~\ref{cap:desenvolvimento} & Atendido \\
10 & Procedência dos dados de treinamento do LLM &
     Cap.~\ref{cap:referencial} (Sabiá-2, Llama 3) & Parcial \\
11 & \textit{Prompt engineering} (versões, iterações) &
     Apêndice~\ref{ape:prompt} & Atendido \\
12 & Parâmetros de geração (temperatura, tokens) &
     Cap.~\ref{cap:desenvolvimento}; código público & Atendido \\
13 & Estratégia de recuperação (chunker, embedding, k) &
     Cap.~\ref{cap:desenvolvimento}; Apêndice~\ref{ape:topk-sweep} & Atendido \\
\midrule
\multicolumn{4}{@{}l}{\emph{Métodos --- avaliação e validação}} \\
\midrule
14 & Métricas quantitativas justificadas &
     Cap.~\ref{cap:avaliacao}; Seção~\ref{sec:interp-faith} & Atendido \\
15 & Replicação multi-seed para variância do juiz &
     Tabelas~\ref{tab:ablacao-2x2}, \ref{tab:hybrid-rerank} & Atendido \\
16 & Viés do juiz LLM (mesma família do candidato) &
     Seção~\ref{sec:ablacao-2x2}; L6 & Atendido \\
17 & Validação humana complementar &
     Seção~\ref{sec:sessao-expert} & Pendente \\
18 & Tamanho amostral declarado &
     Seções~\ref{sec:sessao-expert}, Cap.~\ref{cap:avaliacao} & Atendido \\
19 & Dados ausentes / falhas de avaliação tratadas &
     \texttt{eval/run\_ragas.py}; Cap.~\ref{cap:avaliacao} & Atendido \\
\midrule
\multicolumn{4}{@{}l}{\emph{Resultados e discussão}} \\
\midrule
20 & Resultados com incerteza (DP) &
     Tabelas~\ref{tab:ablacao-2x2}, \ref{tab:hybrid-rerank},
     \ref{tab:gate-final}; Apêndice~\ref{ape:per-category} & Atendido \\
21 & Discussão de viés sistemático da métrica &
     Seção~\ref{sec:interp-faith} (paradoxo) & Atendido \\
22 & Generalizabilidade discutida &
     Cap.~\ref{cap:discussao}; L1--L7 & Atendido \\
23 & Limitações nomeadas explicitamente &
     Seção \textit{Limitações} (L1--L7) & Atendido \\
\midrule
\multicolumn{4}{@{}l}{\emph{Outras informações}} \\
\midrule
24 & Código-fonte e \textit{dataset} públicos &
     Repositório GitHub (Cap.~\ref{cap:metodologia}) & Atendido \\
25 & Privacidade de dados e LGPD discutidas &
     Seção \textit{Compatibilidade do Desenho com LGPD} & Atendido \\
26 & Conflitos de interesse declarados &
     Folha de rosto & Pendente \\
\end{longtable}
}

\noindent\textit{Fonte:} Produção do próprio autor, adaptação parcial da
diretriz TRIPOD-LLM \cite{gallifant2025tripod}. Status \emph{Atendido}
indica cobertura no texto da monografia (com granularidade variável);
\emph{Parcial} indica cobertura ampliável em revisão pós-defesa;
\emph{Pendente} indica item dependente de etapa não executada no escopo
deste TCC.

A checklist é apresentada em forma adaptada para compatibilidade com a
estrutura ABNT da monografia. O texto integral da diretriz TRIPOD-LLM
\cite{gallifant2025tripod} contém itens adicionais sobre validação externa
em populações múltiplas e calibração estatística que excedem o escopo de
um TCC de prova de conceito, sendo mais aplicáveis a versão derivada para
publicação acadêmica.

% ─── Apêndice F — Varredura de top_k ───────────────────────────────────────
\chapter{Varredura de \texttt{top\_k} no Gate Final}
\label{ape:topk-sweep}

A escolha de $k = 5$ para o número de chunks recuperados (descrita na
Seção~\ref{cap:desenvolvimento}) foi originalmente justificada por
observação qualitativa durante a Fase~2. Para fornecer evidência
quantitativa adicional dessa escolha, executou-se uma varredura sobre o
pipeline final (Llama~3.3~70B + BGE-M3 1024D + híbrido + reranker mGTE)
variando $k \in \{3, 5, 7, 10\}$, com as mesmas 38 questões \textit{in-scope}
e o mesmo juiz RAGAS (\texttt{gpt-4o-mini}, temperatura $0$).

\textbf{Limitação metodológica.} A varredura foi executada em \textbf{corrida
única por valor de $k$} (sem replicação multi-seed) para economizar cota
de API. Diferenças em \textit{Faithfulness} entre valores adjacentes devem
ser lidas em comparação com o envelope de variância inter-seed observado
no gate final ($\pm 0{,}004$ a $\pm 0{,}020$, Tabela~\ref{tab:hybrid-rerank});
flutuações abaixo desse envelope não podem ser interpretadas como diferenças
materiais sem replicação. A replicação multi-seed da varredura está
registrada como aprimoramento metodológico para versões derivadas do
trabalho.

\begin{table}[ht]
  \caption{Varredura de \texttt{top\_k} sobre o gate final (corrida única
           por valor; 38 questões; juiz \texttt{gpt-4o-mini})}
  \label{tab:topk-sweep}
  \begin{tabularx}{\textwidth}{@{}Xcccc@{}}
  \toprule
  $k$ & \textbf{Faith.} & \textbf{Ans. Rel.} & \textbf{Ctx. Prec.} & \textbf{Ctx. Rec.} \\
  \midrule
  3                & $0{,}741$ & $0{,}725$ & $0{,}961$ & $0{,}811$ \\
  \textbf{5} (gate) & $\mathbf{0{,}774}$ & $\mathbf{0{,}750}$ & $\mathbf{0{,}961}$ & $\mathbf{0{,}842}$ \\
  7                & $0{,}736$ & $0{,}742$ & $0{,}971$ & $0{,}811$ \\
  10               & $0{,}783$ & $0{,}709$ & $0{,}971$ & $0{,}838$ \\
  \bottomrule
  \end{tabularx}
  \caption*{Fonte: Produção do próprio autor. Pipeline: Llama 3.3 70B +
            BGE-M3 1024D + híbrido BM25+denso (RRF $k_{RRF}=60$,
            \texttt{fetch\_k=20}) + reranker mGTE (\texttt{fetch\_k=20}).}
\end{table}

\textbf{Leitura.} A varredura sustenta empiricamente $k=5$ como escolha
defensável: é o melhor valor em três das quatro métricas
(\textit{Answer Relevancy}, \textit{Context Recall} e empate em
\textit{Context Precision}), e está dentro do envelope de incerteza
inter-seed do melhor valor de \textit{Faithfulness} observado ($k=10$,
$0{,}783$ contra $0{,}774$ em $k=5$ --- diferença de $0{,}009$, abaixo
do DP máximo de $\pm 0{,}020$ reportado na Tabela~\ref{tab:hybrid-rerank}).

A queda em \textit{Answer Relevancy} entre $k=5$ ($0{,}750$) e $k=10$
($0{,}709$, $-5{,}5\%$) é o padrão esperado de introdução de ruído com
mais chunks: candidatos adicionais aumentam cobertura mas diluem foco
da resposta. A pequena melhora em \textit{Context Precision} de $k=3,5$
para $k=7,10$ ($0{,}961 \to 0{,}971$, $+1{,}0\%$) reflete o efeito de
reranker mGTE: com mais candidatos para reordenar, há mais oportunidade
de subir chunks relevantes para o topo.

\textbf{Conclusão.} \emph{$k=5$ não é o ótimo absoluto em
\textit{Faithfulness}}, mas é o valor que produz o melhor equilíbrio
entre fidelidade ao contexto, relevância da resposta e cobertura do
ground-truth. A escolha está empiricamente validada para o pipeline
deste trabalho. Replicação multi-seed da varredura, e exploração de
$k$ fracionário via \textit{post-hoc filtering} sobre $k=10$ pelo
\textit{reranker}, são linhas registradas como aprimoramento
metodológico futuro.

\end{apendicesenv}

\end{document}
